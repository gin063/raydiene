# CHANGELOG

## 2026-08-25（五）— 全部完成；唯一待观察点：2026-09-01 首次自动续期

微信内置浏览器 hero 视频问题**已实测秒开**（用户真机确认）。全链路收尾状态：

| 项 | 状态 |
|---|---|
| moov faststart + H.264（保留 1080p60） | ✅ |
| poster 补齐（原 `hero-poster.jpg` 是 404） | ✅ |
| SSR HTML 里带 `src`（黑屏根因） | ✅ 已上线 |
| CDN 接入 + Range 回源 2MB | ✅ |
| `Cache-Control: public, max-age=31536000, immutable` | ✅ 实测生效 |
| CDN 证书自动续期（acme.sh `ali_cdn` hook） | ✅ 已配置，待首次实战 |

### CDN 证书续期配置结果

VPS 上确认 acme.sh **v3.1.3**，`deploy/ali_cdn.sh` 在该 tag 已存在，无需升级。
`Ali_Key` 已存在于 `account.conf`（用 `dns_ali` 验证），无需重复配置。执行：

```bash
export DEPLOY_ALI_CDN_DOMAIN="assets.raydiene.cn"
acme.sh --deploy -d assets.raydiene.cn --deploy-hook ali_cdn --ecc
```

读 v3.1.3 源码确认了两点：`DEPLOY_ALI_CDN_DOMAIN` 会被 `_savedeployconf` 持久化（第 48 行）；
即使没存也会回退成证书主域名（第 50 行），双保险。

### ⚠️ 续期日期是 9-01，不是 10 月

`acme.sh --list` 显示续期周期是 **30 天**（非 acme.sh 默认的 60 天）：

- `assets.raydiene.cn`：Created 2026-08-03 → **Renew 2026-09-01**
- `raydiene.cn`：Created 2026-08-06 → **Renew 2026-09-04**

**9-01 是 `ali_cdn` hook 的第一次实战**。主站续期脚本虽已稳定运行很久，但从未走过这个 hook，
是全新一环，需实际验证。当天之后跑：

```bash
echo | openssl s_client -connect assets.raydiene.cn:443 -servername assets.raydiene.cn 2>/dev/null \
  | openssl x509 -noout -dates -serial
```

基准值 serial `A2111968CED4CB59E063F8A61E0907AD` / `notAfter Nov 1 2026`——
两者都变化才说明「签发 → 部署到 CDN」闭环成立。

另需确认 `Le_DeployHook` 已落盘（否则自动续期不会触发 CDN 部署）：

```bash
grep -iE "DeployHook|ALI_CDN" ~/.acme.sh/assets.raydiene.cn_ecc/assets.raydiene.cn.conf
```

### 遗留（非阻塞）

- `public/videos/_original-hevc/` 留有原始 HEVC 母带，OSS 上的旧
  `video-mobile.mp4` / `video-pc.mp4` 确认无引用后可删
- 本地 `e:\Code\23363672_assets.raydiene.cn_nginx\` 是 2026-05-03 已过期的 DigiCert 证书，
  含私钥文件，未清理

## 2026-08-25（四）— CDN 切换完成；Hero 修复已上线；待办：Cache-Control + CDN 证书续期

### 已完成并实测确认

- **DNS 已切到 CDN**：`assets.raydiene.cn` → `assets.raydiene.cn.w.kunlunaq.com`
  实测 `Server: Tengine` + `Via: kunlun3.cn9405`，Range 请求返回 `206` ✅
- **Range 回源已开启**，分片 2MB（控制台确认）
- **Hero 黑屏修复已上线**，线上 HTML 已含
  `src="https://assets.raydiene.cn/videos/video-mobile-v2.mp4"` ✅

### 待办 1：Cache-Control 仍未设置

新版 OSS 控制台入口**不叫「设置 HTTP 头」，叫「设置文件元数据」**——在文件列表勾选文件后
底部那排按钮的第一个。填 `public, max-age=31536000, immutable`。

因为 CDN 已经缓存了无 Cache-Control 的响应，设完还要去 **CDN → 刷新预热**按 URL 刷新两个视频地址。

### 待办 2：CDN 侧证书自动续期（11-01 到期，务必在此前完成）

主站和 OSS 都有自动续签脚本，唯独 CDN 侧没有。到期当天 CDN 上的证书不会自动更新 → 资源全挂。

证书是 **ZeroSSL ECC DV** —— 这是 acme.sh 从 v3.0 起的默认 CA + 默认密钥类型，
**推断续期脚本就是 acme.sh，但未证实**（脚本不在本机，需登服务器跑 `acme.sh --version` 确认）。

若确为 acme.sh，一次性执行即可，之后每次续期自动部署：

```bash
export Ali_Key="LTAI..." Ali_Secret="..."
export DEPLOY_ALI_CDN_DOMAIN="assets.raydiene.cn"
acme.sh --deploy -d assets.raydiene.cn --deploy-hook ali_cdn --ecc
```

- **`--ecc` 不能省**：证书是 ECC，acme.sh 存在单独目录，不加会找不到
- hook 会写进该域名的配置文件，后续续期自动触发，无需重复设置
- 该 hook 调的是 `SetCdnDomainSSLCertificate`（endpoint `cdn.aliyuncs.com`）。
  **DCDN 是另一个 hook `ali_dcdn`**，我们用的是 CDN，别选错
- RAM 子账号权限：`AliyunCDNFullAccess`（必需）、`AliyunCASFullAccess`、
  `AliyunDNSFullAccess`（若用 DNS-01 验证）

验证基准（续期成功后这两个值都会变）：
serial `A2111968CED4CB59E063F8A61E0907AD`，到期 `Nov 1 23:59:59 2026`。

```bash
echo | openssl s_client -connect assets.raydiene.cn:443 -servername assets.raydiene.cn 2>/dev/null \
  | openssl x509 -noout -subject -dates -serial
```

## 2026-08-25（三）— assets 域名接入 CDN（DNS 已于当日切换完成，见上一节）

`assets.raydiene.cn` 原为直连 OSS（`Server: AliyunOSS`，cn-shanghai，无 CDN）。已在 CDN 控制台
完成域名添加与证书配置，**但 DNS 还没切，线上仍走 OSS**。

### 关键信息（下次接手需要）

- CDN CNAME：`assets.raydiene.cn.w.kunlunaq.com`（边缘 IP 实测 124.232.182.6）
- **回滚值**：`raydiene-assets-zrs.cn-shanghai.taihangpkx.cn`（OSS 原 CNAME），TTL 10 分钟
- DNS 里 `assets.raydiene.cn` 是**独立托管的 zone**，所以主机记录是 `@` 而不是 `assets`。
  同一主机记录不允许两条 CNAME —— 是改现有那条，不是新建
- `_dnsauth` TXT 是域名归属验证记录，不要动

### 证书

CDN 控制台的证书下拉是 `cert-5yj3sh` / `cert-1kkk03` / `cert-lspaaq` 这种无意义 ID，
认不出谁是谁。**用 SNI 直连边缘节点即可在切 DNS 前验证选对没有**：

```bash
openssl s_client -connect assets.raydiene.cn.w.kunlunaq.com:443 \
  -servername assets.raydiene.cn 2>/dev/null | openssl x509 -noout -subject -dates -ext subjectAltName
```

已验证选中的是正确那张：`CN=assets.raydiene.cn` / ZeroSSL ECC DV / 2026-08-03→**11-01**。
另外两张分别是主站证书（SAN 只有 `raydiene.cn`+`www`，**不覆盖 assets**）和一张过期的 DigiCert。

- 本地 `e:\Code\23363672_assets.raydiene.cn_nginx\` 那对 pem/key 是 **DigiCert，2026-05-03 已过期**，
  别再用；私钥文件还留在磁盘上，未删
- **续期风险**：证书 11-01 到期。现有自动续期脚本（不在本机，未找到 acme.sh/certbot/lego/win-acme 痕迹）
  只更新 OSS 侧。若 CDN 侧走「自定义上传」贴死 pem/key，到期当天资源全挂。
  当前选的是「数字证书管理服务」，需确认续期脚本会把新证书推送到那里

### 切换前已用 `--resolve` 绕过 DNS 完整验证过 CDN 链路（对线上零影响）

```bash
curl -sI --resolve assets.raydiene.cn:443:124.232.182.6 https://assets.raydiene.cn/videos/video-mobile-v2.mp4
```

- 走 CDN ✅ `Server: Tengine` + `Via: kunlun7.cn6209`
- 回源 ✅ `x-oss-cdn-auth: success`
- **Range ✅ `206` + `Content-Range: bytes 0-1000/11195384`**（iOS Safari 的硬需求）
- 缓存 ✅ 首次 MISS → 二次 `HIT TCP_MEM_HIT`
- 冷对象首次 Range：TTFB 0.755s / 1MB 用时 0.84s

### 待办

1. **给 OSS `videos/` 下两个 v2 文件补 `Cache-Control: public, max-age=31536000, immutable`**。
   现在响应里一个缓存头都没有，CDN 只能用自己的默认值（实测 `X-Swift-CacheTime: 2592000`，30 天），
   浏览器端则走启发式缓存。敢设一年是因为文件名带版本号，换内容就换 v3。
   **要在切 DNS 前做**，否则还得去 CDN 刷新缓存
2. 控制台确认 **Range 回源** 是「开启」（默认关闭）。外部实测行为正常，但开关状态无法从外部确认
3. 改 DNS CNAME → 切换
4. **Hero 黑屏修复仍未部署**，线上还是旧代码

## 2026-08-25（二）— 修微信首次进首页 Hero 黑屏；src 必须进 SSR HTML

### 症状

视频换 H.264+faststart 上线后，PC / 移动端 Chrome & Safari 都秒开，但微信内置浏览器：
**首次进首页黑屏、视频完全不加载**；点进任意内页再点 logo 返回首页，则秒加载并自动播放。

### 根因：`<video>` 在 SSR 输出的 HTML 里没有 `src`

线上实测拿到的标签（注意没有 src，且 `preload="none"`）：

```html
<video poster="/images/hero-poster-mobile.jpg" autoplay muted loop playsinline
       webkit-playsinline="true" x5-video-player-type="h5-page" preload="none" ...>
```

`src` 全靠 `onMounted` 里 JS 赋值，凑成一条死链：

1. HTML 解析出一个**没有 src 的 video** → `networkState = NETWORK_EMPTY`，原生 `autoplay` 无从触发
2. 要等 Nuxt 的 JS chunk 下载完 + hydration 完成，`onMounted` 才设 src
3. 此时早已脱离页面加载的初始上下文，微信的自动播放策略拦掉 `play()`
4. `preload="none"` 意味着浏览器也不会自己去拉 → **零字节下载，黑屏**
   （`preload="none"` + 无 src 时 X5 连 poster 都不渲染，所以是黑屏而非静止画面）

**从内页返回之所以正常**：客户端路由，JS 早在内存里 `onMounted` 立即执行；
更关键的是用户此时已点过两次，微信的自动播放限制在用户手势后解除。

### 改动（`components/TheHero.vue`）

- **`src` 改为 SSR 就带上**（`heroSrc` ref 初值即竖版 OSS 绝对地址），浏览器一解析 `<video>`
  就开始加载、原生 autoplay 直接生效，完全不依赖 JS。已验证预渲染 HTML 里有 src
- `preload="none"` → **`preload="metadata"`**。有 autoplay 时浏览器会为满足播放继续拉，
  所以移动端等价于 auto；PC 在换 src 前只白拉约 54KB metadata（faststart 后 moov 在头部）
- PC 换横版时**同步写 `el.src`**，不是只改 ref。ref 要等 Vue flush 才落到 DOM，
  中间空隙里 `el` 上还是竖版地址
- **删掉了无条件的 `el.load()`**。移动端调它会把浏览器已拉到的数据全丢弃重来；
  PC 上它还有个时序 bug——在 ref flush 前执行，实际重载的是旧的竖版
- **新增用户手势兜底**：`touchstart` / `click` 各挂一次 `{ once: true }` 补播，
  微信非 WiFi 下仍会拦自动播放，此时用户随便点一下即播
- `WeixinJSBridgeReady` 监听补 `{ once: true }`；新增 `onBeforeUnmount` 清理三个监听器——
  首页 ⇄ 内页来回切正是本次的复现路径，未触发的监听器会持续堆积

### 待办 / 未验证

- **微信真机未实测**（本地无法复现 X5 行为）。根因是线上 HTML 实测 + 症状推理得出，
  需上线后在微信里验证「首次进首页」这条路径
- CDN 仍未挂，`assets.raydiene.cn` 依旧 `Server: AliyunOSS` 直连（见下一节的开通步骤）

## 2026-08-25 — 修微信内置浏览器首页 Hero 视频起播 30 秒

### 根因不在「微信特殊写法」，在视频文件本身

`TheHero.vue` 里的 `playsinline` / `webkit-playsinline` / `x5-video-player-type="h5-page"` /
`WeixinJSBridgeReady` 兜底**原本就写对了**，不是问题所在。实测两个 hero 视频：

| 检查项 | 修前 |
|---|---|
| moov atom 位置 | **文件末尾**（mobile: offset 14,856,958 / 总长 14,895,890） |
| 编码 | **HEVC / H.265**，1080p，60fps |
| OSS Range | `Accept-Ranges: bytes` 正常（不是服务端问题） |

**主因：moov 在文件尾部（未 faststart）。** 播放器拿不到 moov 就解不了任何一帧。
桌面 Chrome/Safari 会先发一个尾部 Range 请求把 moov 抓回来所以秒开；
微信 X5 内核的视频代理层**顺序拉流**，必须把 14MB 整个下完才摸到末尾的 moov —— 对应那 30 秒。

**次因：HEVC。** 微信 X5 对 H.265 支持极差，多数机型走软解，1080p60 软解起播还要再慢一截。
iOS Safari 和桌面 Chrome 都有硬解，所以本地测不出来。

**第三个坑：`heroPoster` 指向的 `/images/hero-poster.jpg` 根本不存在（404）。**
所以那 30 秒是纯黑屏，poster 兜底完全失效。

### 改动

- 两个视频重转 **H.264 High / 保留 1080p / 保留 60fps / `-movflags +faststart`**
  （moov 已在 offset 32）。H.264 效率不如 HEVC 但原片码率本就保守，体积反而降了：
  mobile 14.9 → 11.2 MB，pc 15.6 → 13.3 MB
- **改了文件名 `video-{mobile,pc}-v2.mp4`**（不是同名替换）。微信 X5 视频缓存很顽固，
  同名覆盖大概率测到旧文件，误判"修了没用"。原 HEVC 存于 `public/videos/_original-hevc/`
- 从视频首帧生成 `hero-poster-mobile.jpg`(54KB) / `hero-poster-pc.jpg`(74KB)。
  `heroPoster` 默认竖版 —— 预渲染 HTML 里就带上它，微信打开无需等 hydration 即可出画面；
  PC 在 `onMounted` 换横版（桌面白吃 54KB，但桌面本来就快，换的是微信首屏）
- `isHeroPlaying` 改为监听真实 `play`/`pause` 事件。原来是点击时乐观翻转，
  一旦微信拦截自动播放（非 WiFi 下会），图标状态就是反的，第一次点还会对已暂停的视频调 `pause()`

### 待办 / 未验证

- **视频未上传 OSS**：需把 `public/videos/video-{mobile,pc}-v2.mp4` 传到
  `assets.raydiene.cn/videos/`，poster 两张随站点部署即可。**上传前线上是坏的**（v2 文件 404）
- **微信真机未实测**（本地无法复现 X5 行为），根因是静态分析 + ffprobe 实测推出来的。
  上线后需在微信里验证起播时间
- `assets.raydiene.cn` 直连 OSS 无 CDN（`Server: AliyunOSS`）。本次不是瓶颈（Chrome 秒开说明带宽够），
  但跨地域访问会有波动，值得后续接 CDN
- 旧的 `video-mobile.mp4` / `video-pc.mp4` 确认新版没问题后可从 OSS 删除

## 2026-07-30 — 修金属表面「氧化/水雾」质感；玻璃 pass 改 scissor 裁剪

### 「氧化 / 水雾 / 油污」= 平坦法线图上的 JPEG 噪声

坚石银灰表面那种粗糙不均匀的痕迹，根因是 KeyShot 烘出来的**法线贴图**：

- 13/43 材质带 `normal` 贴图，尺寸 100×33 ~ 3328×2991
- 实测通道均值 **127.7 / 126.8 / 254.1**（完全平坦是 128/128/255），
  **88% 的像素偏离平坦 ≤3 个单位** —— 也就是说这些图**不携带任何真实凹凸信息**
- 但它们存成 **JPEG**：色度子采样 + 8×8 块效应把「平坦」变成了 ±几个单位的噪声
- 这点扰动落在 metalness 0.89~0.97 的**近镜面金属**上，被镜面反射放大几十倍
  → 表面出现水雾 / 油污 / 氧化般的斑块

新增 `stripFlatNormalMaps()`，**按平坦度判定后再剥**而不是无条件删除
（阈值：偏离 >5 的像素占比 < 35%）。将来对方若真烘了拉丝、磨砂之类的细节法线，
这里会自动保留。

五款共剥掉 **49 张**平坦法线图，顺带把总体积从 **11.11 → 6.95 MB**。

| 模型 | 处理后 | 剥掉的法线图 |
|---|---|---|
| 坚石 Pro | 1.98 MB | 13 |
| 磐石 Pro | 0.84 MB | 11 |
| 磐石 Max | 1.14 MB | 9 |
| 星辰 | 1.48 MB | 8 |
| 星耀 | 1.51 MB | 8 |

### 突破 60fps：玻璃 pass 改按分组 scissor 裁剪

**不动分辨率、不动 MSAA 等级**（用户明确要求）。四项优化：

**① 玻璃着色器只在面板所在区域运行**（最大的一项）

原来是一个全屏三角形，玻璃着色器跑在全部 620 万像素上（1760×880 @2dpr）——
可面板只覆盖约一半画面，剩下的算力全扔了。每个玻璃像素要做
2~3 次 SDF 场求值 + SPECTRAL 次纹理采样，非常贵。

改成两趟：
- **全屏廉价拷贝**（新增 `COPY` 着色器）：每像素 1 次采样，过渡期 2 次
- **按分组 scissor**：玻璃着色器只在各组包围盒（+60px 投影边距）内跑

按当前桌面布局估算：8 个分组，覆盖 ≈53% → **省掉约 47% 的玻璃片元开销**。
HUD 新增实时覆盖率读数。

⚠️ `setScissorTest` 是 renderer 的**持久状态**，pass 结束必须关掉，
否则下一帧的场景渲染也会被裁掉。

**② 梯度 4 抽头 → 2 抽头**
原来用中心差分求 SDF 梯度（4 次 `field()`）。改前向差分并复用已算出的中心值 `d`，
只需 2 次。SDF 的梯度模长恒为 1，前向差分精度足够。

**③ 光谱采样 6 → 3**
默认色散只有 0.03，6 次采样的偏移彼此只差 3%（12px 厚度下约 0.4px），
纯属浪费。**若把 `uDispersion` 调到 0.06 以上，需要改回 6。**

**④ 模糊链 1/2 → 1/4 分辨率**
磨砂本来就是重模糊，降采样看不出差别，两趟高斯的像素量少 4 倍。
采样步长同步从 2.0 降到 1.0 以维持相近的模糊半径。

### 环境

上个 session 残留的 dev server 还占着 3011，导致新实例被挤到 3000。
已清掉两个，现在单实例在 `http://192.168.0.127:3011/lab-3d`。

### 已验证 / 未验证

**已验证：** 五款重跑，49 张平坦法线图剥离；总体积 11.11 → 6.95 MB；
`/lab-3d` SSR 200 无 Vue warn；`glassPass.js` 静态检查（COPY 着色器、copyMat、
scissor 开关成对、SPECTRAL 3、2 抽头梯度、1/4 模糊链、23 个 uniform 全对上、括号配平）；
五个 GLB 均 200。

**未验证：** 金属表面斑块是否真的消失；帧率是否突破 60（**待用户看 HUD**）；
scissor 边界处是否有可见接缝（60px 边距应该够，若见到框状痕迹就是边距不足）。

### 若还不够快，下一步的杠杆

1. `MARGIN` 60 → 45（投影实际只延伸 54px，还有余量）
2. 收紧布局让分组包围盒更小 —— 覆盖率直接决定这项优化的收益
3. `EXT_meshopt_compression` 替代 Draco（解码更快，但主要省的是加载时间不是帧时间）
4. 最后才考虑降 `samples` 4→2

---

## 2026-07-29（二）— ⚠️ 撤销并重写「朝内网格修复」；定位「划痕」为 Draco 量化不足

### ⚠️ 上一版的 `fixInvertedMeshes()` 是错的，已造成实际损坏

用户报「相较在线预览有破洞、色块偏移、划痕」。逐条排查后确认
**破洞与暗块是我上一版引入的**，不是模型问题。

**错在哪：** 判据是「有符号体积 < 0 且法线朝外比例 < 50% → 整块翻转」。
但 KeyShot 按材质/图层切分实体，导出的网格**大量是开放面**：

| 被我翻转的网格 | 三角面 | 边界边 | 实际性质 |
|---|---|---|---|
| 坚石 mesh#15 | 188,912 | **8,150** | 开放面 |
| 坚石 mesh#17 | 19,124 | 3,625 | 开放面 |
| 坚石 mesh#16 | 18,562 | 3,427 | 开放面 |
| 坚石 mesh#14 | 692 | 130 | 开放面 |

**开放面的有符号体积毫无意义**，而「法线相对网格中心朝外」对包裹型壳体也没有分辨力
（实测三款该指标都落在 50~67% 的噪声区间）。于是我把坚石的**主壳**（188,912 面）
连同磐石 Pro 1 个、磐石 Max 3 个网格全部错误翻转 → 破洞与暗块。

**真实情况：** 三款「绕序与存储法线一致率」为 **99.55% / 99.82% / 99.73%**
—— 厂家的绕序和法线本来就一致，**没有任何系统性翻转需要修**。

### 改为 `alignWindingToNormals()`：逐三角形、以法线为准

法线来自 NURBS 曲面定义，是权威。若某三角形 `cross(e1,e2)` 与其存储法线反向，
就交换两个索引。这样绕序处处与法线一致，**不依赖任何「闭合 / 朝向」的全局猜测**。

加了相对阈值 `cos < -0.05` 才修 —— 夹角接近 90° 的薄面 `cross·normal` 趋近 0，
符号纯属数值噪声，翻它没意义还破坏幂等性。

实测修正量：坚石 0.37%、磐石 Pro 0.17%、磐石 Max 0.24%、星辰 0.12%、星耀 0.11%。

### 恢复 `doubleSided`

原先「实体产品不需要双面，省一半填充」的优化也是错的：**大量网格是开放面**，
强制单面就会露出破洞。在线预览器观感干净，正是因为它用的是 KeyShot 原样的
`doubleSided: true`。已移除这一步。

### 「划痕」= Draco 法线量化位数不足

`draco()` 的默认量化对产品级光滑曲面**明显不够**：

| 位置 / 法线 | 坚石体积 | 位置网格 | 法线精度 |
|---|---|---|---|
| 14 / **10**（默认）| 2.31 MB | 0.025 mm | **5.63°** ← 划痕来源 |
| 14 / 12 | 2.53 MB | 0.025 mm | 2.81° |
| **16 / 14**（采用）| 2.93 MB | **0.006 mm** | **1.41°** |

5.63° 的法线在大面积光滑壳体上会 snap 成可见刻面条纹。改用 16/14 后，
残余的绕序不一致也从 809 → 246 个（0.06%，都是亚像素薄面，无视觉影响）。

若仍有带状条纹，下一步换 `EXT_meshopt_compression`（默认 16 位、解码更快，
代价是前端要额外加载 meshopt 解码器）。

### 体积代价

五款 9.32 → **11.11 MB**（渐进加载下首屏仍只等一款）。

| 模型 | 原始 | 处理后 | 面数 |
|---|---|---|---|
| 坚石 Pro | 43.50 MB | 2.93 MB | 391,107 |
| 磐石 Pro | 22.20 MB | 1.26 MB | 200,361 |
| 磐石 Max | 31.69 MB | 1.80 MB | 275,931 |
| 星辰 | 33.29 MB | 2.58 MB | 314,432 |
| 星耀 | 34.64 MB | 2.55 MB | 328,477 |

### 教训（写给后续 session）

**不要用整块几何的统计量去推断朝向。** CAD 导出的网格既非闭合也非凸，
有符号体积、质心朝外率这类全局指标都不可靠。逐三角形、以厂家法线为准
才是唯一稳的做法。判断「是否闭合」必须数边界边（且要先按坐标焊接，
因为 KeyShot 顶点不共享，按索引数出来 100% 都是边界）。

### 已验证 / 未验证

**已验证：** 五款重跑完成；幂等残差收敛到 0.06% 量级；
`doubleSided` 确认保留；`/lab-3d` SSR 200 无 Vue warn；五个 GLB 均 200。

**未验证：** 破洞 / 色块 / 划痕是否真的消失（**待用户在浏览器确认**）。

---

## 2026-07-29 — 坚石重导版：源头未修，脚本兜住；新增 GLB 体检工具

### 坚石新版仍有同样问题

对方重导了一版（29.59 MB → **43.50 MB**，面数 261k → **391k**，看着是提高了细分精度）。
体检结论：**朝内网格一个没解决，而且更严重。**

| | 旧版 | 新版 |
|---|---|---|
| 三角面 | 261,274 | 391,107 |
| 朝向正常的网格 | 24/32 | **39/43** |
| 朝内网格 | 4 个，最大 67,155 面 | 4 个，**最大 188,912 面** |
| 顶点/三角面 | 3.00（零共享）| 3.00（零共享）|
| doubleSided | 32/32 | 43/43 |
| 烘死的 AO | 32 | 43 |

`mesh#15` 一块就有 188,912 面且法线朝外仅 45% —— 这是主壳，难怪整体发黑。
**根因仍是 CAD 转网格时部分实体法线方向没统一，重导多少次都一样**，
必须在源头（KeyShot / CAD 里统一法线方向）或由我们的脚本兜住。

我们的 `fixInvertedMeshes()` 已自动翻正 4 个，处理后复检报 0。
尺寸与朝向正常：主壳宽 186mm 对标称 186.5 ✓，+Z 侧突出 189mm / −Z 侧 0mm
→ 正面朝 +Z，无需旋转。

处理结果：**43.50 MB → 2.31 MB**（材质 43→19，贴图 58→15）。
五款合计 9.32 MB。

### 新增 `scripts/inspect-glb.mjs`（只读体检工具）

```
node scripts/inspect-glb.mjs <文件.glb> [标称宽 高 深(mm)]
```

之前每次诊断都靠临时脚本，**随 session 清理就没了，反复重写**。固化成项目工具，
覆盖这条链路上反复出现的所有问题：

- 顶点是否共享（`顶点/三角面 = 3.00` 即 KeyShot 原始导出的零共享特征）
- **绕序 / 法线是否朝内** —— 逐网格列出面数与法线朝外比例
- 世界尺寸、朝向判定（对比标称，提示是否需要 `rotateY`）、
  主壳外突出量（判断哪一侧是贴墙背面）
- doubleSided / 烘死的 AO / 完美镜面材质计数
- 贴图与几何的体积占比

⚠️ 对 Draco 压缩过的文件无法解码逐顶点数据，会明确提示「请对原始导出文件运行」。

### 环境变化

局域网 IP 从 `192.168.1.5` 变为 **`192.168.0.127`**，
预览地址 `http://192.168.0.127:3011/lab-3d`，二维码已重新生成到 scratchpad。

### 已验证 / 未验证

**已验证：** 坚石新版处理后复检无朝内网格；`/lab-3d` SSR 200 无 Vue warn；
五个 GLB 均 200 且体积正确；体检工具在原始文件上输出完整。

**未验证：** 坚石在浏览器里是否真的不再破面发黑（**待用户确认**）。

### 给对方的反馈要点

如果希望从源头修好（避免每次都靠脚本兜）：

1. **统一法线方向** —— CAD/KeyShot 里对所有实体执行一次「统一法线朝外」
2. 关掉 AO 烘焙
3. 勾 Draco 压缩
4. 单面输出
5. 细分精度不必调这么高 —— 391k 面对网页偏重，中等精度即可
6. 材质起有意义的名字（现在全是「默认值 / 新材质 002 / 图层 20」）

---

## 2026-07-28（八）— 接入坚石与星耀，定位并修复「破面 / 黑黢黢」

### 坚石的「破面 + 黑黢黢」= 网格绕序朝内

同事反馈「三次导出来都是破面的，源文件正常，导出来黑黢黢的」。解码顶点数据实测：

| | 磐石 Pro（对照）| 坚石 | 星耀 |
|---|---|---|---|
| 法线非单位长度 | 0 | 0 | 0 |
| 法线 NaN | 0 | 0 | 0 |
| 退化三角形 | 0.16% | 0.16% | **0.00%** |
| **绕序朝外的网格** | 38/39 | **24/32** | 34/34 |
| 绕序与法线一致 | 99.82% | 99.36% | 99.87% |

**坚石有 8 个网格有符号体积为负**，其中 4 个法线也同步朝内
（网格 2/3/4/5，mesh#3 有 67,155 面，是最大的几块之一）。

成因链：KeyShot 全量导出 `doubleSided`，**掩盖了绕序问题**；一旦改单面，
朝内的面被背面剔除 → 「破面」；而法线同样朝内 → 光照按背面算 → 「黑黢黢」。
**不是同事的操作问题，是 CAD 转网格时部分实体的法线方向没统一。**

### `optimize-glb.mjs` 新增 `fixInvertedMeshes()`

对每个 primitive 翻转绕序 + 法线取反。判据必须**两条同时成立**：

1. 有符号体积 < 0
2. 法线相对网格中心朝外的比例 < 50%

⚠️ 只看体积会**误伤开放曲面** —— 它们的有符号体积本来就没有意义。
坚石里有 4 个这样的面（网格 21/27/28/29），法线其实是对的，绝不能翻。

**顺带在磐石 Pro 揪出 1 个、磐石 Max 揪出 3 个**朝内网格 —— 之前一直静默存在。
五款重跑后自检（对输出再跑一遍）全部报 0，修复干净。

### 星耀：几何最干净，「怪」在朝向

星耀 34/34 网格朝外、**零退化三角形**，是五款里几何质量最好的。
主壳实测 `130 × 368 × 248` mm，与星辰**完全相同** —— 同样是 X=深 / Z=宽，
绕 Y 轴转了 90°，枪同样在 +Z。所以 `rotateY: 90`，与星辰一致。

坚石主壳 `186 × 385 × 75`，宽度 186 对标称 186.5 ✓，枪在 +Z、背面 0 伸出
—— 朝向与磐石一致，**无需旋转**。

### 五款全部接入

| 模型 | 原始 | 处理后 | 面数 | rotateY |
|---|---|---|---|---|
| 坚石 Pro | 29.59 MB | 1.56 MB | 261,274 | — |
| 磐石 Pro | 22.20 MB | 1.03 MB | 200,060 | — |
| 磐石 Max | 31.69 MB | 1.49 MB | 275,622 | — |
| 星辰 | 33.29 MB | 2.26 MB | 314,432 | 90 |
| 星耀 | 34.64 MB | 2.23 MB | 328,477 | 90 |

合计 8.56 MB，渐进加载下首屏只等其中一个。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；五个 GLB 均 200 且体积正确；
翻转修复的幂等自检五款全部为 0；坚石/星耀的主壳尺寸与标称吻合。

**未验证：** 坚石修复后是否真的不再破面发黑（**这是本次最需要你确认的**）；
星耀转 90° 后的朝向；星耀「怪」是否还有几何以外的问题。

### 遗留

- 星耀整体 Z 跨度 581mm，比星辰的 415mm 多出 166mm —— 线缆摊得更开，
  可能影响构图，必要时可裁掉线缆末段
- `MATTE` 仍是全局单套
- 生产环境 `public/models/*.glb` 应挪到 OSS

---

## 2026-07-28（七）— 面板打光提亮、非对称机型按中轴居中

### 面板过暗：三处一起补

黑玻璃面板、logo、枪头、线缆在多数角度下糊成死黑，中央 HUD 尤其明显。

**1. 屏幕与灯带改为自发光。** 真机的屏幕是发光的，GLB 里却只是一张深色 baseColor 贴图，
纯靠环境光照永远提不亮。凡是带 `baseColorTexture` 的材质一律
`emissiveMap = map` + `emissiveIntensity 0.9` —— 在这批模型里带 baseColor 贴图的
恰好就是屏幕与灯带（磐石 Pro 那两张 82×81 的小图是呼吸灯），一举两得。

**2. 新增跟随相机的补光。** 原来两盏定向光是**世界固定**的，模型自动旋转到某些角度时
正面完全背光。`headLight` 挂在 `camera` 上（`camera.add(headLight)`，并把 camera 加进
scene 否则子光源不参与渲染），保证「朝向观众的那一面」永远有基础照度。
日间 0.55 / 夜间 0.75，已并入 `THEMES` 与 `applyTheme` 的补间。

**3. 非金属材质放宽。** `dielectricEnv` 0.45 → 0.75、`dielectricRoughness` 0.55 → 0.40。
日间 `envIntensity` 0.95 → 1.1。

### 非对称机型的旋转中轴

星辰/星耀的枪向右伸出 167mm，用**整体包围盒**中心居中会把机体推离舞台中轴，
旋转起来明显不对称。

改为**分轴处理**：

- **横向 (X/Z) 按主壳居中** —— 主壳取包围盒体积最大的网格，不依赖命名
  （KeyShot 导出的节点名没有语义）
- **纵向 (Y) 仍按整体包围盒** —— 否则下垂的线缆会出画

实测修正量：

| 模型 | 主壳中心 vs 整体中心 |
|---|---|
| 磐石 Pro | X 0mm，Z **−46mm** |
| 磐石 Max | X 0mm，Z **−100mm** |
| 星辰 | X **−84mm**，Z +5mm |

磐石两款横向本来就居中，修正的是深度（不再把前伸的枪算进中心）；
星辰修正的正是横向那 84mm。

⚠️ 接触阴影是 `holder` 的子物体，会跟着上面的偏移一起移动，
所以它的局部坐标要把横向偏移**反向抵消**掉，才能始终落在舞台中轴正下方。

`loadPileModel` 新增 `center` (Vector3) 与按主壳计算的 `bodyBottom`；
proxy 没有 `center`，`layoutFor` 里退回只偏 Y 的老路径。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；两个模块编译通过；
`THEMES` 两套字段对齐（新增 `head` 后共 11 项）；`applyTheme` st/to 对齐；
三款主壳中心与整体中心的偏差已量化（见上表）；proxy 无 `center` 的退回路径存在。

**未验证：** 提亮后是否够看清细节、会不会过曝；自发光强度 0.9 是否合适
（**若屏幕发白过曝，调 `loadPileModel.js` 里 `MATTE.emissive`**）；
星辰居中后的实际观感。

### 遗留

- 星耀未到货；到货后同样需要 `rotateY`，且同属非对称机型
- 磐石两款的 Z 向居中变了（不再含前伸的枪），若觉得靠后可微调
- 帧率：磐石 Pro 63 / Max 67 / 星辰 65

---

## 2026-07-28（六）— 接入磐石 Max 与星辰，修星辰朝向

### 两款新数模

| 模型 | 原始 | 处理后 | 面数 | 材质 | 贴图 |
|---|---|---|---|---|---|
| 磐石 Max | 31.69 MB | **1.49 MB** | 275,622 | 47→19 | 58→10 |
| 星辰 | 33.29 MB | **2.26 MB** | 314,432 | 33→12 | 47→11 |

与磐石 Pro 完全同一套问题：KeyShot 导出、无压缩、**顶点零共享**
（827,793÷275,931 与 943,296÷314,432 都正好等于 3.0）、全双面、烘死的 AO、
外带 9 个和 2 个无用相机。`scripts/optimize-glb.mjs` 直接吃下，无需改动。

### ⚠️ 星辰朝向错误（绕 Y 轴转了 90°）

星辰机体实测 `X=130(深) Y=368(高) Z=248(宽)`，而标称是 235W × 365H × 120D
—— **宽度落在 Z 轴、深度落在 X 轴**，整个模型转了 90°。磐石两款正常。

判定正面朝向绕了些弯路，记录一下哪些判据**不可用**：

- ❌ **看截图** —— 空闲 3 秒后模型会自动旋转，截图是任意角度，不是默认机位
- ❌ **最暗的大面板** —— 会命中背板（背板同样是纯黑材质），磐石对照直接判反
- ❌ **底部网格的深度轴均值** —— 量到的是下垂的线缆，不是枪

有效判据是**「背面贴墙，主壳外突出量为 0」**：

| 模型 | 深度轴 | 主壳外突出量 | 结论 |
|---|---|---|---|
| 磐石 Pro | Z | +Z 207mm / −Z **0mm** | 正面 +Z ✓ 无需旋转 |
| 磐石 Max | Z | +Z 143mm / −Z 58mm | 正面 +Z ✓ |
| 星辰 | Z（宽度轴）| +Z 167mm / −Z **0mm** | 枪在 +Z |

⚠️ 星辰这条要配合用户提供的实物照片才能读对：**星辰/星耀的充电枪装在机体右侧**，
不像磐石那样嵌在正面。所以那 167mm 的侧向伸出量是枪，而非正面突出。
由「右 = Y × 正面」反推得正面朝 **−X**，需绕 Y 轴 **+90°**。

`loadPileModel` 新增 `rotateY` 选项（度），在量包围盒**之前**应用，
产品数据里 `rotateY: 90`。**星耀到货时大概率也需要同样处理。**

### 渐进加载

三个 GLB 合计约 4.8 MB。原本 `Promise.all` 全部 await 完才 `ready`，
真实网络下会卡好几秒的「正在装载」。改为：

- 只 `await` 首屏那一款，其余后台并发加载
- 新增 `reveal(key)` 统一处理「推上屏 + 同步 shownKey + 触发文案重跑」
- `swap()` 遇到未就绪的模型直接跳过，由 `ensureModel` 完成时补一次 `reveal`
  —— 覆盖「用户在后台加载期间就切了过去」的竞态
- 渲染循环里 `models[shownKey.value]?.ledMat` 加了可选链

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；三个 GLB 均 200 且体积正确；
两个模块编译通过；三款世界包围盒单位均为米、Y 轴朝上；
磐石两款宽度 0.187/0.188m 对标称 186.5mm 吻合；旧的 `ctx.layoutFor` / 手动
`shownKey` 赋值已无残留。

**未验证：** 星辰旋转 90° 后是否真的正面朝前（**若看到的是侧面或背面，
把 `rotateY: 90` 改成 `-90` 或 `180` 即可**）；三款各自的哑光观感；
275k / 314k 面下的帧率（磐石 Pro 200k 时约 65fps）。

### 遗留

- 星耀尚未到货；到货后大概率同样需要 `rotateY`
- `MATTE` 仍是全局单套，三款金属处理不同可能需要分别调
- 呼吸灯仍未接

---

## 2026-07-28（五）— 昼夜过渡动画、Apple 风格开关、夜间提亮

### 背景必须改成自绘层才能做过渡

`scene.background` 只能挂一张贴图，换的瞬间是硬切，**结构上做不了交叉淡入**。
改为自绘全屏层：一个 `PlaneGeometry(2,2)` + `ShaderMaterial`，顶点着色器直接输出
裁剪空间坐标，片元里两张贴图按 `uMix` 混合。`renderOrder = -1` + `depthTest/Write: false`
保证它先画且不写深度。

⚠️ 输出加了 `#include <colorspace_fragment>` —— 裸 ShaderMaterial 不会自动做色彩空间
转换，不加会与内置材质走两套约定，背景亮度对不上。

### 过渡：3D 与 DOM 两侧同步 0.55s

`applyTheme(t, dur)` 把**背景混合、环境强度、主/补光强度与颜色、接触阴影透明度、
玻璃的 tint/tintColor/edge/rim/shadow** 全部打进一个 GSAP 补间，
`ease: power2.inOut`。任何一项硬切都会露馅，所以是一起补的。

DOM 那侧（文字色、描边、开关轨道）用 CSS `transition` 承担，时长对齐 0.55s。
注意：**CSS 自定义属性本身不可补间**，但引用它的 `color` / `background-color` 可以 ——
变量瞬时跳变，实际渲染值仍然平滑，效果等价且零成本。
配套把内联 `:style="{ background: T.hair }"` 改成 `.hair` 类，否则挂不上 transition。

### Apple 风格开关

轨道 3.25×1.75rem + 旋钮 1.375rem，`translateX(1.5rem)` 右移。
曲线用 **`cubic-bezier(.32,.72,0,1)`** —— iOS 的 sheet 缓动，起步快收尾稳。
旋钮内嵌 ☀/☾，配色由主题的 `knob` / `knobInk` 驱动。
`role="switch"` + `aria-checked`，位移靠属性选择器触发。
`prefers-reduced-motion` 下去掉位移补间。

### 夜间提亮 + 默认改日间

墙面 `#0d1116→#0a0d11` 提到 `#1d2530→#19212b`；环境强度 0.4 → 0.6；
暗角 0.5 → 0.42；补光 0.5 → 0.55 且偏冷。默认主题 `night` → `day`。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；SFC 与 scoped CSS 均编译通过，
`cubic-bezier(.32,.72,0,1)` / `translateX(1.5rem)` / `transition: color .55s` 都出到了 CSS；
`THEMES` 两套字段完全对齐（10 项）；`applyTheme` 的 st/to 键完全对齐；
8 个依赖标识符均定义于 `applyTheme` 之前（427~512 行 vs 524 行）；
`scene.background` 已无任何赋值；3D 侧 0.55s 与 CSS 侧 0.55s 一致。

**未验证：** 交叉淡入的实际观感；`colorspace_fragment` 是否让自绘背景与
原 `scene.background` 亮度一致（**若发现切换后整体偏暗或偏亮，就是这里**）；
夜间提亮后是否还够沉浸。

### 遗留

- `MATTE` 仍是单套，夜间可能需要单独一组（暗底允许更低 roughness）
- 磐石 Pro 呼吸灯仍未接
- 帧率 65 左右；瓶颈实测在玻璃 pass 的逐像素 SDF 求值，不在带宽

---

## 2026-07-28（四）— 影棚昼夜切换、修背景网格色带

### 背景「像素化网格」的根因

影棚背景是一张 **256×256 的 CanvasTexture**，被拉到 3520×1760（1760×880 @2dpr）
—— **13.75 倍放大**。双线性插值在纹素边界处二阶导不连续，眼睛会读成一层网格；
再叠加 8-bit 渐变本身的色带。之前纯白高亮时对比度低看不出来，配色一变深就暴露。

修法三件套：

1. 底图 256 → **1024**（放大倍率降到 3.4×）
2. **逐像素抖动噪声 ±2/255** —— 消色带的标准手段，把量化台阶打散
3. `generateMipmaps = false` + `minFilter = LinearFilter`（背景永远放大采样，
   mipmap 只占内存不起作用）

### 影棚昼夜切换（替代一味压制高光）

**方案评估结论：采纳，且夜间设为默认。**

用户看到的高光**本来就是金属的正确行为** —— 一味压制反而让产品显得死板像塑料。
病根是白色环境：金属的颜色来自反射，环境有多亮它就有多亮。
深色影棚里银灰金属反射的是暗环境，高光自然收敛成点缀而非眩光，
这正是产品摄影常用的打光思路。按钮放在爆炸图腾出来的位置。

`THEMES` 常量集中管理两套，切换时**贴图 / 布光 / 环境强度 / 玻璃染色 / 文字色**一起换：

| | day | night |
|---|---|---|
| 墙面 | `#b9c4d0 → #aeb9c6` | `#0d1116 → #0a0d11` |
| 环境强度 | 0.95 | 0.40 |
| 主光 | 1.5 | 2.2（暗底环境贡献少，主光要更强撑形体）|
| 玻璃染色 | 0.26 → 白 | 0.30 → `#121821` |
| 接触阴影 | 1.0 | 0.35（暗底上黑影没意义）|
| 文字 | `#141a21` | `#eef2f7` |

两套影棚贴图**预先生成**，切换只换引用，不重新逐像素绘制（1024² 抖动要遍历 100 万像素）。

### 配套：玻璃染色改为可配色

着色器原本写死 `mix(col, vec3(1.0), uTint)`，只能往白染 —— 暗底下会把卡片染成白块。
新增 `uTintColor` uniform，日间往白染、夜间往 `#121821` 染。

文字色改用 CSS 变量（`--ink` / `--ink-dim` / `--hair`）由主题驱动，
组件内已无硬编码色值。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；两个模块编译通过；
着色器 23 个 uniform 声明与提供全对上；`THEMES` 两套字段完全一致
（含 `glass` 子字段 `tint/tintColor/edge/rim/shadow`）；
`applyTheme` 依赖的 6 个标识符均定义于其之前（422~481 行 vs 489 行）。

**未验证：** 抖动后色带是否真的消失；夜间影棚下产品与玻璃 UI 的观感；
两套主题切换时是否有遗漏未同步的视觉元素。

### 遗留

- `MATTE` 五个值仍是估的，夜间模式下可能需要另一组（暗底允许更低的 roughness）
- 磐石 Pro 呼吸灯仍未接
- 主题切换目前是硬切，没有过渡动画

---

## 2026-07-28（三）— 修「哑光改成了白塑料」、影棚重调、RT 格式纠错

### ⚠️ 上一版哑光化的方向搞反了

原外壳材质是 `baseColor (0.563,0.583,0.629) / metalness 0.967 / roughness 0.33`
—— 一块**中灰金属**。上一版为了去反光把 `maxMetalness` 压到 0.35，结果产品变纯白。

**根因**：PBR 里金属没有漫反射项，颜色完全来自「反射环境 × baseColor」。
压低 metalness 等于把金属变成用 baseColor 做漫反射的塑料，
而 0.563 线性 ≈ sRGB 0.78，配高亮白影棚直接糊成白色。

**正确策略：哑光靠 roughness，不靠压 metalness。** metalness 决定的是材质**类别**，
不是反光强度。同理金属件的 `envMapIntensity` 也不能压太狠 —— 压狠了金属会发黑，
因为它没有漫反射兜底。

改成按金属/非金属分两支：

| | roughness 下限 | envMapIntensity |
|---|---|---|
| 金属（metalness ≥ 0.5） | 0.58 | 0.85 |
| 非金属（黑面板 / 线缆） | 0.55 | 0.45 |

`metalness` 一律保持厂家原值不动。

### 影棚重调

纯白高亮有两个后果：长时间看刺眼；**金属反射的就是这片白，环境越白产品越发白**
—— 金属的颜色来自反射，背景亮度直接决定产品亮度。

- 墙面渐变整体压低到中性浅灰蓝（`#b9c4d0 → #ccd5df → #dde3ea → #aeb9c6`）
- 地面亮池不再拉到纯白（`rgba(246,249,252,0.72)`）
- **新增暗角**，视线收拢到中心，四周的玻璃卡片也更容易读出来
- 主光挪到更侧（`(0.9,1.9,1.5)` → `(1.6,1.7,0.9)`）—— 正打会让圆柱形机身
  没有明暗过渡，形体读不出来；补光压到 0.45 只负责把暗面从死黑里拉一点，不参与塑形

### RT 格式纠错（顺带的性能修复）

场景 RT 原本是 `HalfFloatType` + `SRGBColorSpace` —— **这个组合本身不成立**：
sRGB 要求 `SRGB8_ALPHA8` 内部格式，浮点目标承载不了。
而场景 pass 已经做完 ACES 色调映射、输出就是 LDR，浮点精度根本用不上。

改成 `UnsignedByteType` 后，配 4× MSAA 是 **8 vs 32 字节/像素**的带宽差别。

### 帧率问题的答复（不是锁帧）

按用户几次截图的 HUD 读数对照：

| 状态 | fps | 帧时 |
|---|---|---|
| proxy 6.5k 面，无 MSAA | 165 | 6.0 ms |
| GLB 200k 面，无 MSAA | 93 | 10.7 ms |
| GLB + 4× MSAA（HalfFloat） | 58 | 17.2 ms |

165 这个读数排除了 60 帧上限。GLB 本身 +4.7ms，MSAA 再 +6.5ms。
RT 改 UnsignedByte 后应能拿回一部分，具体待实测。

降帧的备用杠杆（按代价从小到大）：
`samples: 4 → 2`（`PileViewer3D.vue` 里 `samples: lowEnd ? 0 : 4`）→
DPR 上限 2 → 1.75 → `gltf-transform simplify` 减面。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；三个模块编译通过。

**未验证：** 哑光参数是否到位（`MATTE` 五个值仍是估的，需对着实物照片调）；
新影棚配色与打光角度的观感；RT 改格式后的帧率回升幅度。

---

## 2026-07-28（二）— 移除爆炸图、修画面模糊、材质哑光化

### ⚠️ 画面模糊的根因：RenderTarget 没有抗锯齿

`WebGLRenderer` 的 `antialias` 标志**只作用于默认帧缓冲，对 RenderTarget 完全无效**。
我们的场景是先渲进 `rtScene` 再做玻璃合成的，所以整个画面一直是**无抗锯齿**渲染 ——
产品表面高光密集，锯齿糊成一团，这就是「在线 GLB 预览器清晰、我们这儿模糊」的原因
（那类预览器直接渲到 canvas，MSAA 生效）。

修法：`new WebGLRenderTarget(..., { samples: 4 })`（WebGL2 多重采样渲染目标）。
低端设备传 `samples: 0` 关掉。

同时给所有贴图设 `anisotropy = renderer.capabilities.getMaxAnisotropy()`，
斜视角下的贴图不再糊。

### 材质哑光化

KeyShot 材质翻译到 glTF 后偏差很大：**外壳导出成 metalness 0.97**（实际是 PC+ABS 塑料），
黑色面板导出成近乎完美的镜面。在高亮白影棚里这两样会互相放大成刺眼高光，观感廉价。

`loadPileModel` 加了 `MATTE` 处理，三个杠杆按影响排序：

| 杠杆 | 值 | 作用 |
|---|---|---|
| `envMapIntensity` | 0.42 | 直接决定反射了多少环境，**去反光最有效的一档** |
| `minRoughness` | 0.62 | 高光被摊开，不再是锐利镜面点 |
| `maxMetalness` | 0.35 | 非金属件不再整体反射环境色 |

配套压低影棚亮度：`environmentIntensity` 1.15 → 0.85，
keyLight 2.1 → 1.35，fillLight 0.9 → 0.7。

### 移除爆炸图

产品没有内部拆解图，功能作废。清理范围：

- `PileViewer3D`：按钮、`exploded` 状态、`toggleExplode()`、`swap()` 里的调用
- `loadPileModel`：整套 `findPartGroups()` 质心径向位移机制（本会话刚写的，现已无用）
- `buildChargingPile`：`explodeOffsets`、`basePositions`、`setExplode()`

全项目已无 `setExplode / explodeOffsets / basePositions` 残留。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；四个模块编译通过；
爆炸图相关标识符全项目清零；五款 proxy 仍能正常构建与 dispose。

**未验证：** MSAA 后的实际清晰度提升；哑光参数是否到位（`MATTE` 三个值是估的，
需要对着实物照片调）；开启 4× MSAA 后的帧率代价 ——
1760×880 @2dpr 的 4×MSAA 缓冲不便宜，**这是本次最大的性能变量**。

### 遗留

- 磐石 Pro 的呼吸灯还没接（GLB 材质无语义名，需要肉眼认出灯带那个 mesh）
- 若掉帧：先降 `samples` 4→2，再考虑 `gltf-transform simplify` 减面
- `public/models/*.glb` 生产环境应挪到 OSS

---

## 2026-07-28 — 接入首个工厂数模（磐石 Pro GLB）

### 来源与体检

工厂给的是 KeyShot「Smart Export → GLB」的产物（`E:\Code\demo\磐石PRO.glb`，22.2 MB）。

**对的地方：** 合法 glTF 2.0；世界坐标 **0.19 × 0.42 × 0.27 米**，Y-up、已落地摆正
（`lux_root` 矩阵已做 mm→m 与 Z-up→Y-up）；宽 0.19m 对标称 186.5mm ✓；
**200,361 三角面**（远好于预期）；39 个独立 mesh 分三组，爆炸图可直接用。

**五个问题（全部在 `scripts/optimize-glb.mjs` 里处理掉）：**

1. **AO 烘得极重** —— 就是当初特意交代「不要烘 AO」那条，对方还是烘了。
   AO 数据在 R 通道，UV 岛内 **R 均值仅 44~80 / 255**，20~57% 的像素 R<32。
   glTF 只读 R 通道，等于把环境光削掉 80%，配我们的实时 IBL 会渲成死板的黑
2. **完全没做顶点合并** —— 601,083 顶点 ÷ 200,361 面 = **精确 3.0**，零共享。
   这是 22MB 的主因（几何占 93%）
3. **没有任何压缩**（`extensionsUsed` 为空）
4. **39 个材质全部 doubleSided** —— 实体产品不需要，白白翻倍片元填充
5. **29/39 材质 rough=0 且 metal=0** —— 完美镜面绝缘体，会渲成「湿塑料」

附带：材质名全是「默认值 / 新材质 002 / 图层 24」，**无语义**，热点标注和爆炸分组
只能靠几何推断。这条脚本补不了，需要对方重导时命名。

### `scripts/optimize-glb.mjs`（新增，可复现）

`weld → 剥 occlusion → 单面 → roughness 兜底 0.25 → 去相机 → dedup → prune → draco`

```
22.20 MB → 1.03 MB   (21×)
面数  200,361 → 200,060   (只掉 301 个退化面)
顶点  601,083 → 175,812   (合并掉 71%)
材质  39 → 19             (剥掉各自不同的 AO 贴图后大量材质变得相同)
贴图  52 → 13
```

**幂等**：对已处理过的文件重跑不会二次破坏（剥 AO 0 / 改单面 0 / 修材质 0），
所以对方重导后可以无脑再跑一遍。

### `utils/loadPileModel.js`（新增）

对外暴露与 `buildChargingPile` **完全相同的接口**
（`root / ledMat / centerY / bodyBottom / setExplode / setFinish / dispose`），
所以 `PileViewer3D` 不需要区分 proxy 与真实数模。

- **爆炸分组不依赖命名**：`findPartGroups()` 沿单子节点链一路下钻，
  找到第一个「有 ≥2 个含网格子节点」的层。实测正确命中
  `lux_root → Default → 充电桩1 004` 下的三组（8 / 14 / 17 个网格）。
  位移方向按各组质心相对中枢的方向做径向推开，与语义无关
- **`setFinish` 是空实现** —— 真实数模用厂家自己的材质，覆写配色等于毁掉还原度
- `ledMat` 返回 `null`（GLB 里没有可呼吸的灯带材质），viewer 侧已加保护

### 接线方式

产品数据加 `model` 字段即走 GLB，没有则退回程序化 proxy；
GLB 加载失败也会**回退 proxy 并打日志**，不会白屏。
五款目前只有磐石 Pro 有真实数模，其余仍是 proxy。

Draco 解码器从 `three/examples/jsm/libs/draco/gltf/` 拷到 `public/draco/`。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；`/models/panshi-pro.glb`(1.03MB)、
`/draco/draco_decoder.wasm`、`/draco/draco_wasm_wrapper.js` 均 200；
两个新模块编译通过；爆炸分组算法在真实 GLB 上模拟命中三组；优化脚本可复现且幂等。

**未验证（必须浏览器）：** GLB 实际渲染观感；剥掉 AO 后材质是否够真实；
径向爆炸方向是否合理；**200k 面 + 玻璃后处理的移动端帧率**（proxy 只有 6.5k 面，
这是 30× 的差距，是这一版最大的性能未知数）。

### 遗留

- `public/models/*.glb` 目前进了仓库；生产环境应挪到 OSS（`assets.raydiene.cn`），
  与视频 / PDF 一致
- 若移动端掉帧，下一步是 `gltf-transform simplify` 减到 60~80k 面
- 等对方重导：关 AO 烘焙、勾 Draco、单面、**材质起有意义的名字**
- `debug` 仍开着；路由仍是 `/lab-3d`

---

## 2026-07-27（五）— 文字染色 bug、全量乱码、毛玻璃通透化、三档响应式

### ⚠️ 修：切换时深色文字被染蓝

`ScrambleText` 的 `.scrambling` 无差别写了
`color: var(--scramble-color, #2d9ed0)`，把本该是深墨色的**产品名、功率数值、
参数值**全染成品牌色。已移除颜色覆写，只保留 `opacity: .82` 的轻微压暗，
颜色完全交给外层继承。

### 全量乱码

`ScrambleText` 新增 `trigger` prop（递增即重跑）。切换产品时 `swap()` 里
`gen.value++`，**内容不变的文本也一起重跑** —— 表头「核心参数」「产品简介」
「最大功率」、参数行的键名、控制台按钮、订购 CTA 全部参与。
共 21 处 ScrambleText，已验证全部接了 trigger。

`KEEP` 集补了 `℃ ～ % ＋ － ◆ ◎ ✦ » 、，。` 等符号，避免读成坏字。

### 滚轮：去模糊、只露一项

- **移除 blur**，仅用透明度区分层级
- 上下各只显示一个候选项；第二项透明度归零，不再露残影
- 透明度曲线改**两段平滑**：靠近中心淡出（让位给上层锐利文本），
  超过一格淡到全无。上一版在 a=0.5 处是 0→0.5 的硬跳变，tween 经过阈值会闪。
  验证：步长 0.005 时最大跳变 0.00417，中心项与第二候选项均为 0
- 容器高度 19rem → 15rem（只需容纳三项）

### 毛玻璃通透化

按 rondesignlab 四张参考的通透度重调：
`tint 0.44 → 0.26`、`frost 0.62 → 0.82`、`shadow 0.16 → 0.20`、`edge 0.045 → 0.03`、
`rim 0.42 → 0.38`、`thickness 13 → 12`。
**染色压低 + 模糊拉高 + 靠投影而非描边分离层次** —— 这是那几张图里卡片"半透明"
而非"乳白"的关键。

### 三档响应式

桌面布局的断点从 `lg`(1024) 上移到 `xl`(1280) —— iPad Pro 竖屏正好 1024，
之前会套用桌面布局导致左列与滚轮压住模型。

| 断点 | 布局 |
|---|---|
| `< md` (768) | 标题 + 模型 + 型号条（贴底居中）+ 控制台/CTA |
| `md ~ xl` | 上述 + 左侧一张紧凑参数卡；型号条移到左下 |
| `xl` (1280+) | 左列三卡 + 滚轮 + ▲▼ |
| `2xl` (1536+) | 再加卖点卡 ×3 与右上简介卡 |

控制台与 CTA 在小屏收窄（高 2.75rem、字号 13px、`whitespace-nowrap`），
避免上一版手机端「爆炸图」竖排断行。渲染读数改 `xl:block`，小屏不再压住控制台。

### 又一次踩到同一个坑

手机端型号条本来写的是 `left-1/2 -translate-x-1/2` 居中 —— 正是上一条自己记下的禁忌
（`pushPanel` 用 `offset*`，**不计 transform**）。改成
`inset-x-0 mx-auto w-fit`，平板端再用 `md:inset-x-auto md:left-6 md:mx-0` 覆盖。

> 这条约束值得单独盯：**任何被 `pushPanel` 测量的元素，
> 从它到 stage 的整条 offsetParent 链都不能用 transform 定位。**

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；两组件编译通过；
`ScrambleText` 已无 color 覆写；21 处 ScrambleText 全部带 trigger；
滚轮透明度曲线连续（最大跳变 0.00417）且中心项/第二候选项均为 0；
`el.style.filter` 已清零；六处响应式断点分布正确；玻璃面板链路无 transform 残留。

**未验证：** 新玻璃参数在亮底影棚下的实际通透度；三档布局的真机观感；
全量乱码同时跑 21 个 rAF 的开销；帧率。

### 遗留

- `debug` 仍开着；路由仍是 `/lab-3d`
- 右上简介卡仍是占位方案
- 星耀 7kW / 21kW 双功率只展示 21kW

---

## 2026-07-27（四）— 环形滚轮、左列对齐、rondesignlab 材质语言

### 滚轮重叠的根因

上一版 `paintDrum` 的透明度写成
`Math.max(0, 1 - a*0.36) * (1 - Math.min(1, a*2))`，
第二个因子在 `a ≥ 0.5` 时恒为 0 —— **所有非选中项透明度为 0，
而选中项（a=0）反而全亮**，于是它和上层那条锐利文本叠成重影。
两个因子的意图刚好写反了。

现在：`a < 0.5 → 0`（中心项交给上层锐利文本），其余 `1 - (a-0.5)*0.46`。

### 滚轮改环形、首尾相接

`wrapDelta(i, pos)` 取模后折进 ±N/2，5 款正好在选中项上下各排两款。
`select()` 沿最短带符号路径 tween，跨越首尾不会倒绕一整圈；
▲▼ 也走取模，没有端点，去掉了禁用态。

已验证：从星耀(4) 点坚石(0) 的 delta 是 **+1 而非 -4**；
连续 ▼ 七次轨迹为 坚石→磐石Pro→磐石Max→星辰→星耀→坚石→磐石Pro→磐石Max，
`drum.pos` 单调递增到 7，不倒绕。

远近层级按 rondesignlab 的手法加了**景深模糊**（`blur(max(0, a-0.5) × 1.7px)`），
不只是透明度。

### 左列与底排对齐

左列容器改 `top-[13rem] bottom-9` 双向定位，三张卡按 flex 比例
（20 : 23 : 57）分配余量 —— **不是只把第三张拉高，三张一起随舞台高度伸缩**。
卡内用 `justify-center` / `justify-around` 撑开，所以卡片变高时是留白变大而非文字漂移。
底排卡片 `h-[6rem]` 配 `bottom-9`，与左列底边严格对齐。

### 字号

- 标题上下的 tier / tagline：新增 `.label-lg`（15px / +0.12em）
- `.label` 12.5px、左列正文 15px、参数行 15px / 15.5px mono
- 功率改**大号轻字重数字**（`clamp(2rem, 2.6vw, 2.75rem)` / weight 300），
  配微型标签 —— rondesignlab 四张截图里反复出现的排版惯例
- 底排三卡字号上调且**双向居中**（`items-center justify-center text-center`）

### 颜色

**只有 3D 机身用各自配色，UI 文字一律品牌色**。
移除了 `--accent` / `--scramble-color` 的按产品覆写，`accent` 字段从产品数据删除。

### 从 rondesignlab 四张截图提炼并采纳的

1. 圆角显著加大（卡片 18 → 26px，舞台 1rem → 1.75rem）
2. 描边几乎不可见 —— `edge` 0.07 → 0.045，靠色调与投影分离层次
3. 大号轻字重数字 + 微型标签的信息层级
4. 景深模糊区分远近（用在滚轮上）
5. **融合胶囊组**（第三张截图的「8 | Activate | 19」）—— 视图控制台与
   订购 CTA 改为同组，靠近时会粘出液态颈部

### 其他

- 移除左下角「拖拽旋转」提示；渲染读数挪到左下角并调暗（临时）
- 右上角新增**产品简介卡**填补空白（`intro` 字段，取自各产品页描述，控制在 48~52 字）
- 画布下方**恢复全系规格表**，页面重新有可爬取的文字内容
- 底排三张卖点卡改 `2xl:flex`（1536+）：窄屏放不下「三卡 + 控制台 + CTA」一行，
  低于 1536 时只保留控制台与 CTA

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；规格表与五款文案在 HTML 中可检索；
两个组件编译通过；环形最短路径逻辑（见上）；
`shown.accent` / 旧提示文字 / 箭头禁用态确认已清除。

**未验证：** 环形滚轮的实际观感与手感；左列 flex 比例在 760px 下限与 920px 上限下的
留白是否都合适；融合胶囊的粘连效果；帧率。

### 遗留

- `debug` 仍开着；路由仍是 `/lab-3d`
- 右上简介卡是占位方案，用户尚未确定该位置最终放什么
- 星耀 7kW / 21kW 双功率只展示 21kW

---

## 2026-07-27（三）— 字号/间距重排、Matrix 乱码、五色区分、Apple 交互准则

### ⚠️ 修掉一个会直接错位的坑

`pushPanel` 走 `offsetLeft/offsetTop`，而 **`offset*` 不计 CSS transform**。
滚轮容器与选中带原本用 `-translate-y-1/2` 居中，玻璃面板会比可见文字低 28px。
已全部改成 margin 居中（`top-1/2 -mt-[Xrem]`），并确认玻璃面板自身及其
**offsetParent 链上**都不再有 translate 居中。

> 约束记下来：**任何要被 `pushPanel` 测量的元素，它到 stage 之间的整条
> offsetParent 链都不能用 transform 定位。**

### 布局

- 顶部标题对齐 `pages/service/install.vue` 的规格：
  标签 `text-xs → text-sm`、h1 `md:text-5xl → md:text-6xl`、说明 `text-sm → text-lg`
- 画布内字号整体上调：微型标签 10 → 11.5px，正文 12 → 13.5px，
  参数值 12.5 → 14px，控制条 13 → 14.5px，产品名改 `clamp(2.25rem, 3.4vw, 3.5rem)`
- 卡片加大：左列 17 → 19rem 宽，卖点卡 11.5 → 13.5rem 宽 / 4.5 → 5.25rem 高，
  间距 `gap-2.5 → gap-3.5`
- **底部卖点卡右移**（`lg:left-[24rem]`），让开左列的横向区间 —— 上一版重叠的根因
- 左列去掉「外壳材质」卡（三卡制），换取纵向余量；舞台高度改
  `clamp(760px,82vh,920px)`，760 是刚好装得下的下限

### 型号选择器重做

- 移除整块白色背板，只留中间一条**半透明液态玻璃带**标记选中项
- 弧面更外凸：perspective 620 → **340**，角距 24° → 26°，半径 96 → 118px
- **禁用滚轮**，仅点击；右侧新增 ▲▼ 快捷按钮
- 移除「型号」标题与计数

### Matrix 式乱码

`ScrambleText` 的中日韩字符池换成**全角片假名** —— 既是《黑客帝国》的标志性字形，
又与中文同为全角、宽度天然对齐（宽度不齐会导致行宽跳动，把卡片里其他内容推着走）。
未落定的字符透出当前产品强调色微光，落定后 CSS 过渡回继承色。

### 五款五色

机身色与 UI 强调色都按产品区分，切换时一眼可辨：

| 型号 | 机身 | 强调色 |
|---|---|---|
| 坚石 Pro | 曜石黑 `#1a1c1f` | `#2d9ed0` |
| 磐石 Pro | 月光银 `#c2c8d0` | `#0ca678` |
| 磐石 Max | 深海蓝 `#1f6d94` | `#4c6ef5` |
| 星辰 | 香槟金 `#b39468` | `#c2831f` |
| 星耀 | 曜石紫 `#4a3f5c` | `#845ef7` |

强调色刻意拉开色相是**为了评估切换效果**，偏艳；定稿时应收敛到品牌色系。
`buildChargingPile` 的第 4 参数由 `FINISHES` 的 key 改为 `{color, metalness, roughness}`
对象 —— 机身色现在是产品属性，不是全局配色档位。

### 采纳的 Apple 交互准则

参考 `emilkowalski/skills` 的 `apple-design`：

- **pointerdown 就给反馈**：`activeKey`（滚轮/高亮，点击瞬间变）与
  `shownKey`（文案/模型，缩到零点才变）拆成两个状态。所有切换按钮改
  `@pointerdown` 而非 `@click`
- **转场可打断**：去掉 `switching` 锁。中途再点别的型号会 `tl.kill()` 后
  **从当前呈现值**接着演，缩出时长按当前缩放折算（`0.46 × max(0.12, scale.x)`），
  连点不会出现「弹回再缩」
- **弹簧参数**：滚轮用 duration 0.42 / `power3.out`，对应指南的
  damping 1.0 / response ≈0.4
- **字距随字号**：大标题 `-0.022em` + `line-height 1.02`，微型标签 `+0.15em`

### Instagram

`rondesignlab` 抓不了 —— 返回 429，且 Instagram 本身有登录墙。**需要用户提供截图。**

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；三模块编译通过；
滚轮的滚轮事件与「型号」标题确认已移除；玻璃面板链路上无 translate 残留；
Node 冒烟五款 —— 机身色正确写入材质，磐石 Pro 确认无屏，
`setFinish` 新签名 / `setExplode` / `dispose` 均通过。

**未验证：** 弧面外凸程度是否够；乱码观感；新间距在 760px 下限高度下是否真的不重叠；
五色在亮底影棚里的实际观感；帧率。

### 遗留

- `debug` 仍开着，上线前关
- 路由仍是 `/lab-3d`，上线前改名
- 强调色需按品牌规范收敛
- 星耀 7kW / 21kW 双功率只展示 21kW

---

## 2026-07-27（二）— 正式页形态：五款型号 / 滚轮选择器 / 连贯转场

### 五款（不是四款）

补上**磐石 Max**。关键发现：从 `useProducts.js` 的「显示配置」字段看，
**磐石 Pro 是智能呼吸灯、没有屏幕**，磐石 Max 才有 4.3 寸高清屏。
这是可建模的真实差异 —— `PILE_VARIANTS.panshi.screen = 0` 走无屏分支，
呼吸灯改成贯穿式长条作为唯一状态交互。安全防护也不同（Pro 26 重 / Max 28 重）。

五款均为 6578 三角面（磐石 Pro 6576，少一个屏幕面片）。

### 画布加宽

原先受 `container max-w-6xl` 限制在 1152px。改为 `max-w-[1760px] px-4 lg:px-8`，
且宽屏下不再用固定宽高比，改 `lg:h-[min(80vh,880px)]` —— 既加宽又不会高过视口。

### 模型显示不完整

线缆挂在机体下方，所以**视觉重心偏下约 0.058m**，用几何原点居中会把下半截切掉。
新增 `pivot / holder` 两层：`holder.position.y = -model.centerY` 把视觉中心搬到原点，
`pivot` 负责旋转缩放（绕视觉中心而非几何中心）。`centerY` 在建模时由 Box3 算好。

### 切换动效改为连贯

上一版用 `fromTo` 在中途重设旋转角度，所以看起来是"瞬间缩小再旋转放大"。
现在**一次整圈旋转贯穿始终**（`+= 2π`，1.16s power2.inOut，落点与起点一致），
缩放 1 → 0.001 → 1，在归零那一刻换模型。
缩到 0.001 而非 0：零缩放矩阵会让法线变 NaN。

### 型号选择器：轮盘 → 垂直滚轮

iOS 选择器 / 机械表日期轮的做法：非选中项按角度排在 3D 鼓面上
（`rotateX(-d·24°) translateZ(96px)`，按角距衰减透明度）。
**选中项单独用一个不带 transform 的层渲染** —— 3D 变换过的元素文字会被重采样发虚，
选中项必须锐利。支持点击 / 滚轮 / 拖拽；滚轮到头时放行给页面，不锁死页面滚动。

### 文字乱码定格动效

新增 `components/ScrambleText.vue`：文本先随机乱码、再从左往右逐字定格。
**乱码字符按目标字符类型取**（中文用中文池、其余用等宽字母数字池）——
混用会导致行宽跳动，把卡片里其他内容推着走。分隔符与空格不参与乱码。
数值仍走 GSAP 计数滚动，字符串走乱码。全部按 delay 错峰。

### 位置固定

所有玻璃卡设**固定高度**（role 5rem / power 4.5rem / specs 12rem / material 4.5rem /
卖点卡 4.5rem / 控制条与 CTA 2.75rem），间距统一 `gap-2.5`。
文案另加约束：`tier` 一律 4 字，`tagline` 一律「4 字 · 4 字」，`role` 一律「◎ + 4 字」，
`specs` 五款同序等长。**双保险 —— 即便将来文案变长也不会推动布局。**

### 移除

- 页面顶部的开发说明、底部的玻璃参数台与全系规格表
- 画布内：左上「雷迪恩产品中心」、右上「全系 N 款」、右下配色切换（暂无可选颜色）
- `FINISHES` 仍保留在 `buildChargingPile.js`，将来有配色再接回来

⚠️ 规格表移除后，**本页几乎没有可被爬取的文字内容**了（只剩标题和一段导语）。
用户已确认此页不承担 GEO 职责，但若将来要它承担，需要把规格重新以 DOM 形式补回。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200，无 Vue warn / Cannot read / is not defined；
被移除的五处内容在 HTML 中确认为 0；三个模块编译通过；
Node 冒烟五款变体 —— 磐石 Pro 确认无屏幕网格，`centerY` / `bodyBottom` 数值合理，
`setExplode` / `dispose` 不抛错。

**未验证：** 滚轮选择器的手感与 3D 鼓面观感；乱码动效节奏；转场连贯度；
加宽后（约 1760px × 880px）的移动端与桌面帧率 —— 画布像素数比上一版增加约 40%。

### 遗留

- `debug` 仍开着（右上角 fps HUD），正式上线前关掉
- 路由仍是 `/lab-3d`，上线前改名
- 星耀 7kW / 21kW 双功率只展示 21kW，未做档位切换
- 星辰 / 星耀的 `buyUrl` 指向站内产品页（只有坚石、磐石有确切京东链接）

---

## 2026-07-27 — 定位为独立整页 + 四款型号切换 + 全面对齐参考布局

### 定位变更

3D 交互不再分散嵌入各产品页，而是**独立一个集中展示页**。
`pages/lab-3d.vue` 现在按最终形态设计，**上线前需要改路由名并去掉页内的玻璃参数台**。
未来只需替换建模文件。

### ⚠️ 修正了之前编造的参数

上一版界面里写的"磐石 Pro / 21kW / 三相 380V"是我凭空编的。
从 `composables/useProducts.js` 取到真实数据后：**磐石 Pro 实际是 7kW**，
374.8×186.5×121mm。四款其实只有两种形态：

| 型号 | 功率 | 尺寸 (H×W×D mm) | 重量 | 外壳 |
|---|---|---|---|---|
| 坚石 Pro | 7 kW | 374.8 × 186.5 × 121 | 3.1 kg | PC+ABS (V0) |
| 磐石 Pro | 7 kW | 374.8 × 186.5 × 121 | 3.6 kg | PC+ABS (V0) |
| 星辰 | 7 kW | 365 × 235 × 120 | 4.9 kg | 钢化玻璃+合金+PC |
| 星耀 | 7/21 kW | 365 × 235 × 120 | 5.6/7 kg | 钢化玻璃+合金+PC |

**教训：做"最终页面"时不要用占位数据，仓库里有单一数据源就去取。**

### `tris = 1` 不是渲染 bug

`renderer.info.render` **每次 `renderer.render()` 都会自动重置**。
一帧跑 4 趟（场景 + 2 趟模糊 + 玻璃合成），读数时只剩最后那次全屏 pass，
而 `FullScreenQuad` 用的是覆盖屏幕的**单个三角形** → 1 call / 1 tri。
修法：`renderer.info.autoReset = false` + 每帧手动 `reset()`，让四趟累加。

### 底部文字发虚的原因

`-translate-x-1/2` 居中：元素宽度为奇数时会落在**半像素**上，文字子像素渲染变糊。
GSAP 入场动画残留的 transform 也会把元素留在合成层上加重这个问题。
两处都改了：居中改用 flex 容器（`offsetLeft` 保持整数），
GSAP 加 `clearProps: "transform"`。

面板测量顺带改成沿 `offsetParent` 链累加到 stage，支持嵌套容器，
仍然不用 `getBoundingClientRect`（渲染循环里读写交替会触发强制重排）。

### 布局全面对齐参考视频

左上栏目标识 / 右上系列计数 / 大标题（位置固定只换内容）/
左列四卡（定位·功率条·核心参数·材质，同组会粘出液态颈部）/
右侧四扇区产品轮盘（选中项旋转到 3 点钟，文字反向补偿保持水平）/
底部卖点卡 ×3 + 视图控制 + 配色 + 订购 CTA。
窄屏隐藏左列与轮盘，用横向型号条替代。

**浮动标签全部删除**（用户判断"不高级不简约"，且参考视频里模型上确实没有任何标注）。
`HOTSPOT_ANCHORS` 随之从 `buildChargingPile` 移除。

### 产品切换动效

- 四款模型**预先全部构建**，切换只改 `visible` —— 免掉每次切换 dispose/GC 抖动。
  单款 6578 三角面，四款合计 26312，可忽略
- 时间线：旋转 +0.55π / 缩放至 0.7 / 全屏模糊拉满（0.3s, power2.in）
  → **在模糊最重时换模型** → 反向旋转入场 / 缩放回 1 / 模糊归零（0.7s, power3.out）
- 全屏模糊靠新增的 `uTransition`：**只作用于面板之外**，UI 卡片保持锐利
  （参考视频里也是主体动、面板不动）。顺带做 7% 的缩放，读起来像变焦模糊
- 左列规格数值用 GSAP 逐项滚动（延迟 0.3s 等模型换完）。
  **要求各型号 `specs` 同序等长** —— 按下标做插值
- 文字内容用 `.anim-swap` + `:key` 强制重建 + 错峰淡入

### 其他改动

- `MAX_PANELS` 8 → 16（当前布局宽屏约 13 块面板）
- `PILE_VARIANTS`：四款参数化，差异在尺寸、屏幕占比、前面板粗糙度
  （钢化玻璃 roughness 0.03 vs PC+ABS 0.18）
- 模板里原先写的 `ref="p.crumb"` 字符串路径 **Vue 不支持**，已全部改为函数 ref

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200，无 Vue warn / Cannot read / is not defined；
四款型号文案与规格均在 HTML 源码可检索；三个模块编译通过；
GLSL 静态检查（括号配平、22 个 uniform 声明与提供全对上）；
Node 冒烟测试四款变体 —— 各 6578 三角面，包围盒与标称尺寸吻合
（超出部分是枪座与线缆，符合预期），`setExplode` / `setFinish` / `dispose` 均不抛错。

**未验证：** 新布局实际观感；轮盘旋转与文字反向补偿；切换动效节奏；
13 块玻璃面板的移动端帧率（上一版 5 块面板时桌面 165 / 移动 60，
面板数翻倍主要影响全屏那次 `field()` 求值）。

### 遗留

- 参考视频 `E:\Code\demo\F806BDXBf7x7hPf2.mp4` **在仓库外**，抽帧存在
  临时目录里也会被清掉。这套设计语言若要长期沿用，建议把视频或关键帧存进项目
- 磐石/星辰/星耀的 `buyUrl` 指向站内产品页（只有坚石有确切京东链接）
- 星耀的 7kW / 21kW 双功率目前只展示 21kW，未做功率档位切换

---

## 2026-07-26（深夜）— 按参考视频重做界面：亮色影棚 + 贴边停靠网格

参考素材：`E:\Code\demo\F806BDXBf7x7hPf2.mp4`（3024×1714 / 60fps / 38s，英雄选择界面）。
用 ffmpeg 抽了 8 帧分析。**注意这个文件在仓库外，别人接手时可能已不在。**

### 从参考里提炼出的三条设计纪律

1. **亮色无缝影棚背景 + 地面柔影**，深色主体在亮底上对比强烈
2. **UI 永远不压主体**。信息全在贴边停靠的卡片里，位置固定不动，彼此绝不重叠。
   整段视频里只有一个浮动标签（主体脚下的名牌）
3. **面板本身不动，只有内容在换**。第 19 秒那帧是转场中：名字带动态模糊横移，
   底部能力卡错峰淡入（第二张半透明、第三张还空着）

### 用户报的三个问题，根因其实是两个

| 报障 | 根因 | 修法 |
|---|---|---|
| 背景全黑观感不清晰 | 舞台渐变 `#141b21→#05070a` 配曜石黑产品 = 黑上加黑；且玻璃底下没东西可折射 | 换亮色影棚（墙面渐变 + 地面亮池 + 接触阴影），布光改影棚双光 |
| 液态融合导致文字叠加灾难 | ①全局 smooth-min 无视分组 ②带文字的浮标在屏幕空间自由浮动 | ①着色器加分组：同组 smin，跨组硬 min ②**模型上只留编号标记点，文字全部收进停靠卡片** |
| UI 间距/位置乱 | 没有布局纪律，浮标和控制条各自为政 | 贴边停靠网格：左上标识 / 右上配色 / 左列规格+详情 / 底部控制台 |

②③ 是同一个根因：**让带文字的面板自由浮动还互相融合**。
参考的做法是文字一律收进停靠卡片，模型上只有标记点 —— 重叠风险从根上消掉，
顺带解决了"展开后位置很乱"。

### 设计上的一个张力（记录取舍）

参考用的是**不透明白卡 + 严格不重叠**，而 liquid glass 要的是半透明与叠压 —— 方向相反。
取舍：**保留玻璃材质，但采用参考的布局纪律**（贴边停靠、不重叠、统一圆角与内边距、
微型全大写字距标签）。玻璃 `uTint` 从 0.05 提到 0.42 保证亮底上深色文字可读。

### `utils/glassPass.js` 新增

- `uGroup[]` —— 面板按组号排序后传入，同组 smooth-min，跨组硬 min。
  **着色器靠数组相邻性判断同组，所以 `setPanels` 里必须排序**，否则漏融合
- `uShadow` / `uShadowY` —— 卡片下沉投影。玻璃外侧再取一次向下偏移的场，
  只在 `d < 44` 的带子里算，不给全屏加负担。这是参考里卡片"浮起来"的关键
- `uEdge` —— 亮底上白色高光边分不出轮廓，补一条极淡暗色定义边

### `components/PileViewer3D.vue` 重做

- 亮色影棚：`studioBackground()` 画墙面渐变 + 地面亮池；重新加回 `contactShadow()`
- 停靠网格：左上产品标识 / 右上配色 / 左列规格卡 + 详情卡（窄屏贴底）/ 底部控制台
- 模型上只有编号标记点（`①②③④`），点击后内容进详情卡。
  标记点不做玻璃面板 —— 6px 半径上做折射毫无意义且抢性能
- 组号分配：标识=0，配色=1，**规格卡与详情卡同组=2**（垂直堆叠，靠近时粘出液态颈部，
  无文字重叠风险），控制台=3
- 入场动效：`gsap.from(.stage-anim, stagger 0.07)`，复刻参考的错峰淡入。
  选择器限定在本 stage 内，否则同页多个 viewer 会互相触发
- 新增 `specs` prop

### 帧率现状

用户实测：**桌面 165fps，微信 / iOS Chrome 均稳定 60fps 无波动**。
后处理开销有富余，暂不需要降采样数。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；`PileViewer3D.vue`、`glassPass.js` 编译通过；
scoped `@apply` 正常出 CSS；GLSL 静态检查 —— 括号配平、单一 main、
21 个 uniform 声明与提供全部对上、分组 min/smin 分支存在。

**未验证：** 新布局的实际观感与响应式表现（窄屏详情卡贴底、左列在 lg 以下隐藏）；
亮底上玻璃参数是否需要再调；入场动效节奏。

---

## 2026-07-26（晚）— 转 WebGL 玻璃 + 手势冲突解法

### 决策变更：3D 交互区的玻璃改用 WebGL，放弃全站复用

用户决定："与其全员平平无奇不如在这个交互界面足够爆炸"。
**取舍是清楚的：放弃全站 UI 材质统一，换取这一个界面的视觉上限。**

WebGL 相对 CSS 解锁的能力（CSS 结构上做不到，不是做得差）：
真菲涅尔、程序化环境反射、6 次采样的连续光谱色散（CSS 只能 3 次凑 RGB，出两道硬边）、
厚度相关的变半径模糊、以及**面板之间的 SDF smooth-min 流体融合** —— 最后这条是
iOS 26 最标志性的转场，也是 "liquid" 的字面来源。

代价：WebGL 看不见 DOM，只能折射自己场景里的像素。所以**所有玻璃 UI 必须落在 canvas 区域内**，
`PileViewer3D` 的控制条/详情卡从 canvas 下方移到了 canvas 内部。
CSS 方案（`LiquidGlass.vue` / `glassDisplacement.js`）**保留不删** —— 将来若恢复全站铺开仍然用得上。

### 手势冲突：不要抢，要给它意义

用户实测反馈：移动端只有左右拖能操控模型，上下滑被系统滚动接管。

这是 `touch-action: pan-y` 的预期行为（让浏览器做方向仲裁）。硬抢（`touch-action: none`）
会锁死页面滚动，代价更大。**解法是把纵向滑动变成有意义的 3D 操作**：
`onScroll` 把 3D 区块的滚动进度映射成相机俯仰角偏移（`scrollPol`，±0.375 rad）。
纵向滑动从"被吃掉"变成"在推进镜头"，冲突消失。

这同时回答了拖了三轮未定的"交互模型"问题：**横滑=方位角自由环视，竖滑=俯仰角/镜头推进**。

⚠️ 只在 scroll 事件里读 `getBoundingClientRect`，渲染循环只读缓存值 ——
读写交替会触发强制重排。同理热点面板矩形用投影算出的坐标而非量 DOM，
静态 UI 用 `offsetLeft/Top`（已相对定位父元素）而非 `getBoundingClientRect`。

### 新增 `utils/glassPass.js`

流程：`场景 → rtScene ─┬─→ 两趟高斯(半分辨率) → rtBlur ─┐`
　　　　　　　　　　　`　　　　　　└──────────────────────┴─→ 玻璃合成 → 屏幕`

- 面板用圆角矩形 SDF 定义，彼此 smooth-min 求并 → 靠近自动粘成一滴
- 最多 8 个面板，`if (i >= uCount) break` 运行时只跑实际数量
- **`d > 1.0` 早退**：绝大多数像素只算 1 次 field，玻璃像素才多算 4 次梯度抽头
- 场景不透明（`scene.background` 挂了竖向渐变 CanvasTexture）——
  玻璃要有东西可折射，而它看不见 canvas 背后的页面。放在 `scene.background`
  而非着色器里合成，是因为折射采样和模糊链能一并拿到，不用在三处各补一次
- RT 的 `texture.colorSpace = SRGBColorSpace`：场景 pass 写 sRGB，采样时 GPU 解码回线性，
  最终 pass 再编码一次 —— 往返正确，不会发灰或过暗

### 静态检查时修掉的两个着色器 bug（未跑到浏览器就发现）

1. **y 轴翻转**：面板坐标是 DOM 口径（y 从顶部往下），`vUv.y` 却是从底部往上。
   不修的话所有玻璃面板会上下镜像错位。已在 `px` 处翻一次，
   并且折射偏移从 px 空间换回 uv 空间时 y 再取反一次。
2. **`smoothstep(1.0, -1.0, d)`**：GLSL 规定 `edge0 >= edge1` 时结果**未定义**，
   不同 GPU 驱动行为不一致。改为 `1.0 - smoothstep(-1.0, 1.0, d)`。

### 已验证 / 未验证

**已验证：** `/lab-3d` SSR 200 无 Vue warn；`PileViewer3D.vue`、`glassPass.js` 编译通过；
GLSL 静态检查 —— 括号配平、单一 main、着色器声明的 17 个 uniform 与 JS 提供的全部对上、
无反序 smoothstep（唯一匹配在注释里）。

**未验证（必须真机/浏览器）：** GLSL 实际编译；玻璃观感；
**加了后处理后的帧率**（之前裸场景 60fps，现多出 1 次 RT 渲染 + 2 趟半分辨率高斯 + 1 次全屏合成，
每个玻璃像素 6 次光谱采样 + 5 次 SDF 场求值）；滚动驱动俯仰的手感与幅度。

### 待办

- `/lab-3d` 已加玻璃参数台（8 个滑杆）。**点开热点让详情卡出现，再把「融合半径」拉大，
  就能看到详情卡和控制条像水银一样粘成一滴** —— 这是 CSS 做不到的那条，优先看这个
- 微信里看到的降级：若那是 **Android** 微信而非 iOS，说明 `supportsSvgBackdrop()` 的 UA 判定漏了它，
  之前算的"27% 待测"要重算。转 WebGL 后不阻塞，但记录备查
- 移动端 APP 材质改造已确认走各 OS 原生方案，移出本仓库范围

---

## 2026-07-26（下午）— 折射观感修复 + 玻璃路线定案

### 定案：liquid glass 走纯 CSS + SVG 路线

依据 GA「技术概览」过去 30 天数据（按**操作系统**口径算，因为 iOS 上所有浏览器都是 WebKit，
按浏览器品牌口径会系统性低估 WebKit 份额）：

| OS | 活跃用户 | 引擎 |
|---|---|---|
| Android | 278 | Chromium |
| Windows | 250 | Chromium |
| iOS | 127 | **WebKit** |
| Macintosh | 24 | 混合（Safari ≈20）|
| Linux | 14 | Chromium |

**WebKit ≈ 147/693 ≈ 21%，Chromium ≈ 78%，Firefox 可忽略。**

决策理由不只是 78%：既然要**全站铺开 + 未来移动端 APP 复用**，复用性就成了首要判据 ——
WebGL 玻璃出不了 canvas，直接出局。21% 拿到的是优雅降级（毛玻璃），不是功能损坏。

⚠️ 数据里的意外发现：**Android Webview 190 + Safari (in-app) 110 ≈ 总流量 43% 来自 App 内置浏览器**
（小红书不发外链，来源大概率是微信扫码/分享；`public/qr`、`public/qrcode` 佐证）。
所以真实分布是：**约 52% 确定真折射、21% 确定降级、27%（Android in-app）需实测**。
待办：用微信扫码打开 `/lab-glass` 实测那 27%。

### 修了什么：只有斜线看得出变形

**现象**（用户在 Chrome 实测截图中发现）：背景字母的斜笔画（如 N 的撇）有明显变形，
水平和垂直线条几乎无感。

**根因**（两个叠加）：

1. 位移方向取的是圆角矩形外法线。在左右直边上法线是纯水平的，且**沿这条边每一点的位移量恒定**
   —— 竖线只是被整体平移，仍是竖线；上下边同理，横线只是上下平移。
   只有圆角处法线才旋转，斜线才吃得到差异化位移。物理没错，观感失败。
2. 四分之一圆倒角轮廓的位移量在边缘发散、往内急剧衰减，**88% 的位移挤在斜面最外侧 25% 的窄条里**，
   实际只剩一根发丝线。

**修法**（`makeDisplacementMap` 新增两个参数）：

- `magnify`（默认 0.07）—— ★ 关键。整体放大，覆盖**包括平坦中心区在内的整个元件**，
  位移量正比于离中心的距离。真透镜内部本就是放大的，玻璃内外线条的尺度差是
  "这里有块玻璃"最直接的信号，且对横竖线一视同仁。
- `spread`（默认 0.75）—— 在圆弧轮廓与余弦轮廓之间插值。0 = 物理准确但只剩发丝线，
  1 = 位移铺满整条斜面。

**验证**（Node stub，300×120 / bevel 26 / ior 1.5 / thickness 16，沿水平中线扫 R 通道）：

| 配置 | 覆盖率 | R 沿中线（相对 128 的偏移） |
|---|---|---|
| 修复前（仅边缘折射） | 51% | `-75 -34 0 0 0 0 0 34 75` |
| `spread=0.75` | 51% | `-89 -66 0 0 0 0 0 66 89` |
| **新默认 +`magnify=0.07`** | **98%** | `40 57 112 81 0 -81 -112 -57 -40` |
| `magnify=0.2` | 98% | `111 112 123 89 0 -89 -123 -112 -111` |

关键差异是**中间那几个 0**：修复前元件内部完全无位移，竖线穿过玻璃时纹丝不动；
修复后全域有连续梯度，竖线被逐段差异化位移 → 可见的尺度压缩。

峰值落在斜面内侧一点（+112）而非最外缘 —— 因为最外缘处 magnify（向内取样）
与边缘折射（向外取样）方向相反、部分抵消。这是薄透镜边缘的真实行为，观感上是
"内侧一圈压缩环 + 向中心平滑放大"，可接受。要调的话动 `magnify` / `thickness` 比值。

### 其他

- `/lab-glass` 背景加了 45°/-45° 斜向网格。正交网格在直边上只会被整体平移，
  单靠它判断折射强度会得出错误结论，两种网格并置才公平。
- 参数台新增 `整体放大 ★` 与 `折射分布 ★` 两个滑杆，排在最前。
  **把「整体放大」拖到 0 就能复现修复前的症状。**
- 默认值调整：`bevel` 22→26，`thickness` 14→16，`blur` 3→2。

### 已验证 / 未验证

**已验证：** `feImage` + data URL 在 `backdrop-filter` 上下文中被 Chromium 正确消费
—— 这条是整个 CSS 方案的技术命门，用户 Chrome 实测截图确认通过 ✓
页面 SSR 200 无 Vue warn，util 与 SFC 编译通过，位移图数学如上表。

**未验证：** 修复后的实际观感（需在 Chrome 重看 `/lab-glass`）；
微信 Android 内置浏览器（那 27%）是否支持；多玻璃元件同屏 + 嵌套的性能开销。

### 新增待办（用户提出的两个后续方向）

1. **全站 UI 铺开** —— 需要先解决：每个元件的位移图是逐像素生成的，
   大量元件同屏时的生成开销与内存；以及 Apple 的自适应染色（采样背景自动切明暗）尚未实现。
2. **移动端 APP 材质改造** —— ⚠️ 关键前置问题：APP 是原生还是 webview 混合？
   - iOS 原生：iOS 26 有系统级 Liquid Glass API（SwiftUI `.glassEffect()` / UIKit `UIGlassEffect`），
     **不要用 web 方案重造**
   - iOS webview 混合：WKWebView，与 Safari 同样拿不到 SVG 折射
   - React Native：没有 CSS 滤镜，需原生模块或 Skia
   这个问题不问清楚，本仓库的 `LiquidGlass.vue` 能不能复用无从判断。

---

## 2026-07-26 — Liquid Glass UI 材质层（Spike 第二部分）

### 改了什么

新增两个文件，仍然不碰任何现有页面/组件/配置，无新增依赖：

- `utils/glassDisplacement.js` — 折射位移图生成器 + 引擎能力探测
- `components/LiquidGlass.vue` — 可复用玻璃表面组件
- `pages/lab-glass.vue` — 材质评估台（`noindex`）

### 为什么这么做

**1. 明确了 liquid glass 是 UI 层的事，不是产品表面材质。**
产品表面等工程文件到手 1:1 还原，是另一条独立的线。早先曾考虑把玻璃 UI 放进 WebGL 场景渲染，被否 —— UI 会被绑死在 3D 生命周期上、无法超出 canvas 边界、无法复用到全站其他组件。**UI 玻璃必须在 DOM/CSS 层做。**

**2. 真 liquid glass 的核心是折射，不是模糊。**
`backdrop-filter: blur()` 只能糊不能弯。折射唯一的 CSS 路径是 SVG `feDisplacementMap`。
位移图按物理推导生成：把元件当成有圆角斜面的玻璃板，斜面处表面有斜率，按 Snell 定律算折射偏移，编码进 PNG 的 R/G 通道（128 为中性）。中心平坦不位移，越靠边缘位移越强。色散靠三次不同 scale 的位移再按通道 screen 重组。

**3. ⚠️ 关键限制：`backdrop-filter: url(#svg)` 只有 Chromium 支持。**
Safari 接受这个属性但**静默丢掉 SVG 部分**，只留平模糊；Firefox 同理。已在 W3C SVG 工作组讨论中（issue #1142），短期无解。
**且 iOS 上所有浏览器都是 WKWebView** —— Chrome iOS（UA 为 `CriOS`）、Edge iOS、Firefox iOS 全都吃不到。所以评估流量时**要看操作系统占比，不是浏览器品牌占比**，Safari 的显示占比会系统性低估 WebKit 真实份额。
探测不能用 `CSS.supports`（Safari 对 url() 返回 true 但实际不生效），只能按引擎判断，见 `supportsSvgBackdrop()`。

**4. 不能加 `isolation: isolate`。**
它会建立 Backdrop Root，导致嵌套在玻璃卡片里的玻璃按钮只能采样到外层卡片自己的像素，而不是页面背景。`backdrop-filter` 本身已建立层叠上下文，该属性冗余且有害。用 `contain: paint` 控制光栅化开销。

**5. 材质台背景刻意做成高频（细网格 + 大字 + 硬色块）。**
纯渐变背景下真折射和毛玻璃看起来一模一样，会得出错误结论。细网格线是折射最好的试纸。

### 当前状态

**已验证：**

- `/lab-glass` SSR 200，全部文案正常渲染，无 Vue warn
- `LiquidGlass.vue` 与 `glassDisplacement.js` 在 Vite 下编译通过
- 位移图数学（Node stub canvas 冒烟测试，300×120 / bevel 22 / ior 1.5 / thickness 14）：
  - 中心 `[128,128]` 中性 ✓
  - 左边缘 `[75,128]`、右边缘 `[181,128]`、上边缘 `[128,75]` — 方向与对称性正确 ✓
  - `feDisplacementMap scale` = 28.53px，非中性像素 44.2%（斜面带子占外圈一圈）✓

**未验证（需要真浏览器）：**

- `feImage` + data URL 在 `backdrop-filter` 上下文中能否被 Chromium 正确消费 —— **这是整个方案的技术关键点，必须先在 Chrome 里打开 `/lab-glass` 确认**
- 折射的实际观感、参数默认值是否好看
- 降级分支的画质差距有多大（页面已内建 A/B 对照，左真折射右降级）
- 多个玻璃元件同屏 + 嵌套玻璃的性能开销

### 待决策（阻塞后续）

1. **玻璃实现路径** — 等官网流量截图（要看 OS 占比，不是浏览器品牌）。三选一：纯 CSS / canvas 内 WebGL 折射 + canvas 外 CSS 混合 / 全 WebGL
2. **3D 区交互模型** — 自由轨道 / 滚动驱动电影感 / 混合。两轮均未定
3. 工厂 3D 数模能否拿到（周一确认）

### 遗留

- 位移图重算是整元件逐像素遍历，已做 90ms 防抖 + 512px 上限。元件尺寸频繁变化的场景（如响应式动画）需要另想办法
- `LiquidGlass` 目前只有白色染色，Apple 的自适应染色（采样背景自动切明暗以保证文字可读）未实现
- 三个 lab 文件（`lab-3d.vue`、`lab-glass.vue` 及其 utils/components）均未挂进导航与 sitemap，不会被预渲染，验收完可整体删除

---

## 2026-07-25 — 3D 产品交互技术验证（Spike）

### 改了什么

新增三个文件，**完全不碰任何现有页面/组件/配置**：

- `utils/buildChargingPile.js` — 程序化充电桩 proxy 模型（纯 three 几何体，零外部资产）
- `components/PileViewer3D.vue` — 3D 查看器（渲染循环 / 手势 / 热点投影 / 降级）
- `pages/lab-3d.vue` — 独立验证页，`noindex`，未挂进导航

新增依赖：`three@0.185.1`（唯一新增，无版本冲突）。

### 为什么这么做

**1. 用代码搓 proxy，而不是 AI 图生 3D。**
手上只有正面/侧面/45° 三张 PNG。摄影测量需要 30+ 张有重叠的环绕照片，三张正交图数学上解不出深度；单图 AI 生成（Tripo/Meshy/混元3D）对充电桩这类硬表面工业品是最差场景 —— 平面起波浪、边糊、屏幕烂。
充电桩本质是参数化简单实体，代码搭建反而更干净，且**比例是对的**（约 300W×420H×130D mm）：现在标定好的相机机位、热点坐标、爆炸位移，等工厂数模到手能直接迁移。用 DamagedHelmet 之类球状占位物，这套相机工作全白调。

**2. 没用 OrbitControls，自写了 ~60 行轨道控制。**
唯一诉求是「横向拖拽转模型 + 纵向让位给页面滚动」。解法是 canvas 上 `touch-action: pan-y` —— 让**浏览器**做手势方向仲裁，比自己判 touchmove 角度稳。但 OrbitControls 在 pointerdown 阶段就接管事件，跟 touch-action 配合不了。将来需要 pan/dolly 再换回官方实现。
配套决定：**禁用滚轮/捏合缩放**，缩放走 UI 按钮 —— 避免两种滚动劫持。

**3. 用 RoomEnvironment 而不是 HDRI。**
程序化 IBL，省一次 ~2MB 请求，绕开跨域/CSP。真上线要更真实的反射再换 Poly Haven 的 .hdr。

**4. 热点文案不进 canvas。**
`buildChargingPile.js` 里的 `HOTSPOT_ANCHORS` 只有坐标和法线，**没有一个字**。文案由 `pages/lab-3d.vue` 以 props 传入，并在页面下方以 `<dl>` 再出现一次。这是整个方案的 GEO 底线：canvas 对爬虫是黑洞。

**5. 热点位置每帧直接写 DOM style，不走 Vue 响应式。**
60fps 下更新响应式对象会每帧触发组件重渲染。

### 当前状态

**已验证：**

- SSR 渲染 200，全部卖点文案（`GB/T 20234`、`IP65`、四个热点标题+描述）出现在 HTML 源码中
- `noindex,nofollow` 生效
- SFC 编译通过，scoped `@apply` 正常出 CSS
- `three/addons/geometries/RoundedBoxGeometry.js` 与 `three/addons/environments/RoomEnvironment.js` 在 Vite 下解析正常（r185 路径）
- Node 冒烟测试：16 mesh / 6578 三角面 / 包围盒 0.397×0.539×0.198 m，`setExplode` `setFinish` `dispose` 均不抛错

**未验证（需要真浏览器 + 真机）：**

- WebGL 实际渲染效果、帧率、发热
- 移动端单指滚页 vs 横滑旋转的手势仲裁
- 无 WebGL 降级路径的实际表现
- 热点投影的遮挡判定阈值（`dot < 0.15` 是拍脑袋定的，可能需要调）

验收清单已写进 `pages/lab-3d.vue` 页面底部，真机对着跑。

### 下一步

1. 真机跑 `/lab-3d`，按页面底部清单逐条验收
2. **周一去工厂设计部要 3D 数模** —— 优先级：ID 外包方的 Rhino/KeyShot 文件 > 结构部门 STEP/IGES > 原生 SolidWorks。这是工期从 2 周变 6 周的唯一变量
3. 数模到手后：减面重拓扑 → GLTF + Draco + KTX2（目标 < 5MB），替换 `buildChargingPile` 为 `GLTFLoader`
4. 集成进 `pages/products/panshi.vue` 时，确认原有 SSR 文字内容 diff 为空

### 遗留

- 降级态目前是 CSS 画的占位块，真上线要换成产品海报图（`components/PileViewer3D.vue` 里有标注）
- `pages/lab-3d.vue` 未挂进导航也未加进 `nuxt.config.ts` 的 sitemap，构建时不会被 `crawlLinks` 发现，不会预渲染。验收完可整体删除三个文件
